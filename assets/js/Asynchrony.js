'use strict';


                                            // SetTimeout() and SetInterval()

// https://uk.javascript.info/settimeout-setinterval


// Що таке WebAPI?
// Що таке планування?

// функції :
// setTimeout - дозволяє виконувати певну дію через якийсь час(встановлений)
// serInterval - дозволяє виконувати цю ж дію з певним інтервалом

// 1. setTimeout приймає два параметри:

//     - перший параметр це колбек-function (обробник TimeHandler);
//     - другий параметр це число (час в мілісекундах), через скільки мілісекунд виконати перший параметр;

// це обробник для setTimeOut (перший параметр)
// function timeOutAction() {
//     console.log('Hello');
// }

// console.log('befor timeout');  // це виконається першим
// setTimeout(timeOutAction,1000);  // ця функція не зупинитть код нижче, вона має виконатись через 1000мсек піля after
// console.log('after timeout');  // ця виконається друга

// так відбувається тому що це Асинхронний код, він виконається лише після всього Синхронного коду. 
// Синхронний код це весь код який не є Асинхронним напевно)))


// 2. setInterval 

// let count = 0;
// function intervalHandler() {
//     console.log(++count);
// }

// console.log('before interval');  // це виконається першим
// setInterval(intervalHandler, 1000);  // ця функція виконається останньою, коли виконається весь Синхронний код і буде повторюватись кожні 1000мсек
// console.log('after interval');  // це виконається другим

// Якщо врахувати що в нас зараз одночасно прописані дві асинхронні функції, то послідовність буде така
// спершу виконається весь синхронний код (тобто всі наші консоль логи в глобальній обл.видимості)
// потім виконається функція setTimeOut бо вона перша в коді
// потім виконається фукція setInterval бо вона нижче по коду, і вона буде виконуватись кожну 1000мсек скільки їй заманеться (вона сама не зупиниться, буде працювати вічно поки відкрита вкладка)

// Чи можна відмінити дію setTimeout?

// і setTimeOut і setInterval повертають число, це як певний ідентифікатор,
// і є зворотня фукція clearTimeout() яка може припините те, що вказано в setTimeout але для того щоб її використати потрібно передати як параметр цей ідентифікатор
// можем зберегти саму функцію у змінну щоб перевірити

// function timeOutAction() {
//     console.log('Hello');
// }

// console.log('befor timeout');  // це виконається першим
// const timeoutId = setTimeout(timeOutAction,1000); // ця не виконається бо її зупинить clearTimeout
// console.log('after timeout');  // ця виконається друга

// clearTimeout(timeoutId) // ця виконається третя 
// - це функція щоб припинити setTimeout, приймає як параметр id setTimeout (це синхронний код)
// тобто коли ми запустимо цей шматок коду, то має виконатись все крім setTimeout, тому що виконається
// весь синхронний код (в т.ч. clearTimeout) який не дасть запуститись setTimeout-у


// Відповідно так само існує синхронна функція clearInterval() 
// яка припиняє те що вказано в setInterval, і так само їй потрібен параметр id

// let count = 0;
// function intervalHandler() {
//     console.log(++count);
// }

// console.log('before interval');  // це виконається першим
// const intervalId = setInterval(intervalHandler, 1000);  // ця функція виконається останньою, коли виконається весь Синхронний код і буде повторюватись кожні 1000мсек
// console.log('after interval');  // це виконається другим

// clearInterval(intervalId);  // якщо її запустити одразу то setInterval взагалі не запуститься, 
// а якщо запустити її наприклад в консолі під час виконання setInterval то clearInterval просто 
// перерве роботу setInterval в момент свого запуску.


// Отже дію можна запланувати (setTimeout, setInterval) 
// і можна її зупинити за допомогою дзеркальних функцій (clearTimeout, clearInterval)


// можна setInterval запустистити на певну кількість ітерацій за допомогою перевіки if у колбек функції:
// let count1 = 0;
// function intervalHandler() {
//     if(count1 === 5) {
//       return clearInterval(intervalId);  // це виконається після 5 setInterval-ів
//     }
//     console.log(++count1);
// }

// console.log('before interval');  // це виконається першим
// const intervalId = setInterval(intervalHandler, 1000);  // ця функція виконається останньою, 5 разів а потм зупиниться
// console.log('after interval');  // це виконається другим

// такими if-ами можна реалізловувати будь які умови, які повинні з'явитись черезз певний час.


// Приклад
// Зробимо кнопку, на яку треба натиснути щоб отримати знижку, і протягом 5 сек кнопка має зникнути

// обробник для setTimeout
// function deleteBtn() {
//     const discountBtn = document.querySelector('button');  // отримуємо кнопку DOM
//     discountBtn.remove();  // видаляємо кнопку
// }

// setTimeout(deleteBtn, 5000);  // запускаєм дію з видалення кнопки через 5 секунд



// // Завдання 1:
// // При нантисканні на кнопку перефарбувати її через 2 секунди (2000 мсек)

// const colorBtn = document.querySelector('.change-color-once');  // отримую кнопку
// // створюю обробник для отриманої кнопки
// function colorBtnHandler(e) {
//     // в обробнику виставляю setTimeout і прямо в ній прописую колбек-функцію зі зміни кольору через 2 сек
//     setTimeout(() => {e.target.style.color = 'red'}, 2000);
// }

// colorBtn.addEventListener('click', colorBtnHandler);  // навішую обробку на кнопку при кліку


// // Завдання 2:
// // Після натискання на кнопку змінювати її колір кожні 2 секунди рандомно

// // створюю функцію яка генерує випадкове число для кожного спектру кольору в форматі rgb і повертає їх
// function getRandomColor() {
//     const r = Math.floor(Math.random() * 256);
//     const g = Math.floor(Math.random() * 256);
//     const b = Math.floor(Math.random() * 256);
//     return `rgb(${r}, ${g}, ${b})`;
// }

// const colorBtnInterval = document.querySelector('.change-color-interval');  // отримую DOM елемент

// // обробник для кнопки
// function colorBtnIntervalHandler(e) {
//     // встановлюю інтервал в 2 сек (колір змінюватиметься кожні 2 сек)
//     setInterval(() => {
//         // кожні 2 сек в event.target(кнопці) змінюється колір який повертається з функії getRandomColor 
//         e.target.style.backgroundColor = getRandomColor();
//     }, 2000);
// }
// // навішую обробник події на кнопку при кліку
// colorBtnInterval.addEventListener('click', colorBtnIntervalHandler);



// рекурсивний (рекуррентний) setTimeout.

// рекурсивний setTimeout іноді кращий за setInterval
// Уявімо, що ти використовуєш setInterval, щоб кожні 2 секунди завантажувати дані з сервера.

// Проблема: Якщо сервер гальмує і відповідає 3 секунди, а інтервал спрацьовує кожні 2 секунди, 
// то твої запити почнуть "нашаровуватися" один на одного, як сніговий ком. Браузеру стане погано.

// Рішення — рекурсія: ми не ставимо жорсткий інтервал, а плануємо наступний крок тільки тоді, коли 
// попередній завершився.

// let timerId = setTimeout(function request() {
//     console.log("Роблю запит до сервера...");
    
//     // Коли запит виконано, плануємо наступний через 2 секунди
//     timerId = setTimeout(request, 2000); 
// }, 2000);


// Завдання 1
// Давай напишемо функцію-таймер. Вона має виводити в консоль числа від 5 до 1, з паузою в 1 
// секунду між ними, а в кінці написати "Бум! 💥".
// Твої інструменти:
// Змінна-лічильник (почни з 5).
// setInterval (щоб повторювати кожну секунду).
// clearInterval (щоб зупинитись, коли дійдеш до 0).
// Підказка: Не забудь зберегти ID інтервалу в змінну, щоб мати змогу його зупинити. 😉

// let count = 5;  // задаємо лічильник
// function timerHandler() {   // створюємо обробник для setInterval
//     if (count > 0) {  // якщо лічильник > 0
//         console.log(count); // спершу вивести count (щоб почати відлік з 5)
//         count--;  // а потім зменшити count, якщо написати навпаки то відлік почнеться з 4
//     } else {  // інакше
//         console.log('Boom!');  // вивести бум
//         clearInterval(timerId);  // відмінити setInterval
//     }

// }

// const timerId = setInterval(timerHandler, 1000)  // задаємо setInterval на кожні 1000мсек 
// // і зберігаємо в змінну щоб отримати id для clearInterval


// Наступний рівень: "Турбо-режим" 🏎️
// Уяви, що наш "таймер" працює занадто повільно, і ми хочемо додати кнопку "Прискорити".
// Завдання:
// Напиши код, де спочатку інтервал працює кожну 1 секунду. Але як тільки лічильник дійде до 3,
//  інтервал має зупинитися і перезапуститися вже кожні 200 мілісекунд (дуже швидко).
// Як це зробити (алгоритм):
// Створюєш перший setInterval на 1000 мс.
// Всередині обробника перевіряєш: if (count === 3).
// Якщо так — робиш clearInterval старого таймера.
// І тут же запускаєш новий setInterval(timerHandler, 200).
// Це покаже, як динамічно змінювати плани браузера на ходу! 🚀📈

// (Підказка: оскільки ти будеш викликати setInterval двічі, змінна timerId має бути доступною 
// для перезапису, тобто оголошеною через let зовні).


// let count = 8;
// let timerId;

// function timerHandler() {
//     if (count > 0) {   // якщо count більше 0 
//         console.log(count); // вивести count
//         count--; 
//         if (count === 3) {  // якщо count досяг 3 то вмикаєм пришвидщення
//             clearInterval(timerId);  // зупиняємо setInterval на 1000мсек
//             timerId = setInterval(timerHandler, 200);  // створюємо новий на 200мсек
//         }
//         count--;  // і лише після цього зменшуємо count
//         // якщо зменшити його як у прикладі то пришвидшення почнеться після того як 3 виведеться через 1000 мсек. виведеться 2 з якого почнеться інтервал по 200мсек
//         // якщо зменшити його перед if(count===3) то пришвидшення почнеться з 3, вона буде першим швидким числом
//         // також при ранььому декременті є імовірність що count не зможе увійти в if(count === 3) тому що число 3 заміниться на 2 перед тим як дійти до умови
//     } else {  // якщо count <= 0  то вивсети повідомл і зупинити setInterval
//         console.log('Boom!');
//         clearInterval(timerId);
//     }
// }

// timerId = setInterval(timerHandler,1000);  // створюємо перший таймер на повтор що 1000мсек



// Завдання "Світлофор"
// 🚦 Завдання: Алгоритм роботи світлофора
// Тобі потрібно написати код, який послідовно виводить у консоль кольори 
// світлофора з певними затримками.
// Умови виведення:
// "Червоний" — має з'явитися в консолі миттєво (синхронно).
// "Жовтий" — має з'явитися рівно через 2 секунди після того, як вивівся "Червоний".
// "Зелений" — має з'явитися рівно через 1 секунду після того, як вивівся "Жовтий".
// 💡 Два способи реалізації (спробуй обидва для практики):
// Спосіб А: "Матріошка" (Вкладені таймери)
// Ти ставиш перший setTimeout для Жовтого, а вже всередині його колбека прописуєш 
// другий setTimeout для Зеленого.
// Плюс: Час другого таймера почнеться тільки тоді, коли спрацює перший.
// Питання: Яку затримку (мс) треба поставити для Жовтого, а яку для Зеленого в цьому випадку?
// Спосіб Б: "Паралельний старт" (Плоский список)
// Ти пишеш обидва setTimeout один за одним у загальному потоці коду.
// Нюанс: Пам'ятай, що обидва таймери потраплять у Web API майже одночасно.
// Питання: Яку затримку (мс) треба вказати для Зеленого, щоб він з'явився через 1 секунду після 
// Жовтого, якщо Жовтий чекає 2000 мс?

// 1 спосіб матрьошка(Nested)
// console.log('red');  // спершу вивожу червоний

// function trafficLightTimer() {  // це callback для першого setTimeout
//     console.log('yellow');  // другим виведеться жовтий (через 2 сек після червоного)
//     setTimeout(()=>{console.log('green');}, 1000);  // це другий setTimeout який виведе зелений через 1 сек після жовтого
// }

// setTimeout(trafficLightTimer, 2000);  // перший setTimeout для виведення жовтого

// 2 спосіб Плоский список (Flat)

// console.log('red');  // першим виведем червоний

// setTimeout(()=>{console.log('yellow');}, 2000);  // через 2 сек після виведем жовтий
// setTimeout(()=>{console.log('green');}, 3000)  // щоб зелений вивівся третім треба сумувати час 
// 2 сек на жовтий + 1 сек на зелений разом 3 секунди з моменту потрапляння в WebAPI
// якщо рядки поміняти місцями (жовтий і зелений) результат не зміниться бо значення має саме час відкладки
// якщо для зеленого написати 1000 delay то першим виведеться зелений і лише потім жовтий


// Завдання: "Настирливий Будильник"
// Уяви, що ти створюєш додаток-будильник. Тобі потрібно реалізувати таку логіку:
// Початок: Як тільки ти запускаєш код, починає працювати setInterval, який кожну 1 
// секунду виводить у консоль: "Дзінь! 🔔".
// Час виходу: Ти розумієш, що не можеш слухати це вічно. Тому тобі треба запланувати 
// одноразову дію (setTimeout), яка спрацює рівно через 5 секунд.
// Фінал: Ця одноразова дія має:
// Зупинити набридливий setInterval (щоб він більше не дзвонив).
// Вивести в консоль: "Нарешті тиша... 🤫".
// Підказка (архітектурна):
// Тобі знадобляться дві змінні для зберігання ID обох таймерів (хоча для setTimeout ID може
//  й не знадобитися, якщо ти не плануєш його скасовувати, але для setInterval — обов'язково!).
// Спробуй написати цей код. Пам'ятай: один таймер працює "циклічно", а інший — чекає в 
// Web API 5 секунд, щоб потім "вбити" перший.


// let alarmId = setInterval(()=>{console.log('Дзінь');}, 1000);

// setTimeout(()=>{clearInterval(alarmId); console.log('Нарешті тиша');}, 5000);






// Практитка Stopwach

// start - запустити накопичення мсек
// reset - скинути накопичені мсек
// stop - зупинити накопичення мсек

// let time = new Date(0);  // через констурктор Date скидаємо час до 0
// // let time = 0;
// let intervalId = null;


// const timeEl = document.querySelector('.time');
// const [startBtn, stopBtn, resetBtn] = document.querySelectorAll('.btn-container > button');

// updTime(time);

// function startBtnHandler () {
//     const DELAY = 10;
//     function tick() {
//         time.setMilliseconds(time.getMilliseconds() + DELAY);
//         updTime(time);
//     }

//     if(!intervalId) {
//         intervalId = setInterval(tick, DELAY);
//     }
// }

// startBtn.onclick = startBtnHandler;

// function stopBtnHandler() {
//     clearInterval(intervalId);
//     intervalId = null;
// }

// stopBtn.onclick = stopBtnHandler;

// function resetBtnHandler() {
//     time = new Date(0);
//     updTime(time);
// }

// resetBtn.onclick = resetBtnHandler;

// function updTime(time) {
//     timeEl.textContent = `
//     ${formatMinutesAndSeconds(time.getMinutes())}
//     :
//     ${formatMinutesAndSeconds(time.getSeconds())}
//     .
//     ${formatMilliseconds(time.getMilliseconds())}
//     `;
// }

// // форматуємо час, якщо число з одної цифри додамо 0 : 5 => 05
// function formatMinutesAndSeconds(m) {
//     return m < 10 ? `0${m}` : m;
// }

// // форматуємо мсек, якщо число з одної цифри додамо 00 : 5 => 005
// // якщо число з двох цифр додамо 0 :  10 => 010
// function formatMilliseconds(ms) {
//     return ms < 100 ? (ms < 10 ? `00${ms}` : `0${ms}`) : ms;
// }





                            //  Асинхронність

// почитати
// https://uk.javascript.info/event-loop
// https://www.youtube.com/watch?v=8aGhZQkoFbQ&t=389s
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Execution_model

// погратись
// http://latentflip.com/loupe/?code=!!!
// https://www.jsv9000.app/

// Блокуючі операції — це функції, які зупиняють виконання всього коду на сторінці 
// до тих пір, поки користувач не виконає певну дію.

// До блокуючих операцій належать:
// •	alert('Текст') — модальне вікно з кнопкою "ОК".
// •	confirm('Питання?') — вікно з кнопками "ОК" та "Скасувати".
// •	prompt('Введіть дані') — вікно з полем для введення тексту.


// Завдання:
// Подивитись код і прописати що відбудеться "під капотом"

// це перше потрапить в WebAPI і буде виводитись що три секунди
// setInterval(() => console.log("Тік-так..."), 3000);

// далі в WebAPI потрапить це, setInterval (3сек) встигне вивестись один раз, після чого 
// setTimeout (5 сек) виведе alert і поки цей alert висить відкритий, на фоні буде
// продовжувати виконувась setInterval (але не виводитись), він буде накопичуватись в 
// черзі. Все що накопичилось в черзі поки alert блокував стек, вивалиться майже одночасно
//  в консоль(соті мсек) а потім знов буде виводитись з рівним інтервалом
// setTimeout(() => {
//     alert("Я заблокував усе!");
// }, 5000);


// Завдання: 
// подивитись код, і пописати в якому порядку виконаються рядки:

// console.log("1. Старт");   // 1. першим виведеться це

// // 4. четвертим виведеться  setTimeout (виведеться після 3 одразу, 
// // бо його таймер "протІкає" в WebAPI(3сек) а в стек залеть після виконання
// //  всього синхронного коду, якщо alert висів 5 сек, значить таймер setTimeout
// //  вже 2 секунди висить в черзі. Значить setTimeout виведеться одразу після виведення "3 Фініш")
// setTimeout(() => {
//     console.log("2. Таймер на 3 секунди");
// }, 3000);

// // 2. другим виведеитьтся alert
// alert("Зупинись, миттєвосте!"); // Уяви, що ти тримаєш це вікно відкритим РІВНО 5 СЕКУНД

// console.log("3. Фініш");  // 3. третім виведеться це


// Мікро-задача: "Гонка таймерів"
// Уяви, що ми запускаємо "циклічний" будильник і "разову" дію, 
// а потім перериваємо їх блокуючим alert.
// Питання для аналізу коду:
// 1.	Скільки разів встигне вивестися "Дзінь" ДО того, як з'явиться alert?
// 2.	Що станеться з setInterval, поки висить alert (3 секунди)? Чи будуть 
// "Дзіні" з'являтися в консолі "під" вікном?
// 3.	Коли ти натиснеш "ОК" (на 5.5 секунді від старту), що ти побачиш у консолі
//  в першу ж мить? (Чи вистрілять накопичені "Дзіні" одночасно, чи вони зникнуть?)


// let count = 0;

// // 1. Кожну секунду збільшуємо лічильник і виводимо його
// const intervalId = setInterval(() => {
//     count++;
//     console.log("Дзінь №" + count);
// }, 1000);

// // 2. Блокуємо Стек через 2.5 секунди
// setTimeout(() => {
//     alert("Пауза!");   // Тримаємо alert відкритим рівно 3 секунди
// }, 2500);

// // 3. Зупиняємо все через 7 секунд
// setTimeout(() => {
//     clearInterval(intervalId);
//     console.log("Гру закінчено");
// }, 7000);

// Відповіді на запитання:
// 1. 2 рази виведеться Дзінь ДО того як з'явиться alert
// 2. SetInterval покладе в чергу ще 1 Дзінь поки відкритим висить alert 
// (лише один не зважаючи на час через Interval skipping, setInterval не
//  буде закидувати в чергу ідентичну таску якщо така ж таска вже чекає в черзі)
// 3. після натискання ОК в alert вистрілить накопичений Дзінь і ще встигне десь 
// два Дзінь вивестись "з поточного setInterval"
// 4. Через 7 секунд з початку всього коду, SetInterval зупиниться. Дзінь в 
// згагальному встигне вивестись 5 разів, також ми подивимось на alert і закінчим
//  повідомленням "Гру закінчено".



// console.trace() - це метод виводить у консоль стек викликів (stack trace). 
// Він показує список функцій, які були викликані, щоб дійти до того місця, 
// де ви поставили цей trace.

// function f1(){
//     console.trace(); // 2. показує стан CallStack в поточний момент (тобто функцію f1 яку викликала функція f)
//     console.log('end');  // 3. це буде в стеку останнім
// };
// function f(){
//     console.trace(); // 1. показує стан CallStack в поточний момент (тобто функцію f)
//     f1();
// }
// f();






// ----------------------------------------------------------------------------------------------------------------------------------


                    // JSON (JavaScript Object Notation)

// https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/JSON
// https://developer.mozilla.org/ru/docs/Learn_web_development/Core/Scripting/JSON
// https://www.json.org/json-uk.html
// https://uk.javascript.info/json




// Devtools -> Network   - статус всього що підвантажуєеться на сторінку (зображення, стилі, код , ...)



// кілька форматів для передачі даних :
// xml
// JSON - найбільш популярний для передачі даних і налаштування config-id
// yaml  - що це ?


// в JSON можуть бути такі типи даних:
// Obejct (звичайний plainObject - обєкт без методів/функцій)
// array
// String
// Number
// true/false
// null
// числа, булеві і налл  не потрібно брати в лапки!!!

// в JSON НЕ можуть бути функції/методи, undefined, Symbol, BigInt, !!
// Функції переддавати можна в скрипті якщо треба!


// обєкт перед відправко треба загорути в JSON і при отриманні розгорнути з JSON в обєкт.
// для цього в JS є глобальний обєкт JSON який має два методи:
// 1. JSON.parse() - розбирає рядки JSON і перетворює на обєкт
// 2. JSON.stringify() - навпаки, запаковує (складає) дані з обєкта в рядки JSON


// const obj = {
//     number: 1, 
//     string: 'Hello',
//     array: [1, 'hi', [1], false, null, {oops: true}],
//     boolean: true,
//     null: null,
//     plainObject: {prop: 10},

//     // методи не можна передавати
//     getNumber() {
//         return this.number;
//     },
//     // undefined не можна передавати 
//     undefined: undefined
// };


// obj => JSON  - серіалізаціяя
// const objJson = JSON.stringify(obj);
// console.group(objJson);
// {
//     "number":1,
//     "string":"Hello",
//     "array":[1,"hi",[1],false,null,{"oops":true}],
//     "boolean":true,
//     "null":null,
//     "plainObject":{"prop":10}

        // методу getNumber тут нема!!! Методи не можуть бути запаковані(розпаковані)!
        // властивості undefined тут теж нема!!! Undefined не може бути запакований(розпакований)!!
// };


// JSON => obj - десеріалізація

// const parsedObj = JSON.parse(objJson);
// console.log(parsedObj);
// {
//     number: 1, 
//     string: 'Hello',
//     array: [1, 'hi', [1], false, null, {oops: true}],
//     boolean: true,
//     null: null,
//     plainObject: {prop: 10},

//     методів та undefined тут не може бути !!!

// }

// Щоб дані передавати по мережі, їх треба спершу перетворити на валідний JSON.




                    // JSON Error Handling

// Більшість методів у JS просто повертають null чи undefined якщо щось іде не так. 
// Але JSON дуже суворий до синтаксису  (як видно з попереднього розділу). 
// Будь яка помилка в синтаксисі під час виконання JSON.parse() викличе помилку 
// SyntaxError яка повністю зупияє виконання скрипта. Уникнути цього можна 
// за допомогою конструкції try…catch.

// try {
//   // 1. Зона ризику: код, який може "вибухнути"
//   const data = JSON.parse('{"name": "Ivan",}');     // Ой, висяча кома!
//   console.log(data); 
// } catch (error) {
//   // 2. Зона порятунку: спрацює ТІЛЬКИ якщо в try сталася помилка
//   console.error("Сталася помилка парсингу!");
//   console.log("Ось що пішло не так:", error.name);    // Назва помилки (SyntaxError)
//   console.log("Деталі:", error.message);             // Опис (Unexpected token } in JSON...)
// } finally {
//   // 3. Зона фіналу: спрацює ЗАВЖДИ (і при успіху, і при помилці)
//   console.log("Спроба парсингу завершена.");
// }



// Стратегії поведінки в catch

// Просто вивести помилку в консоль — це добре для розробника, але погано для користувача. 
// Що робити в catch на практиці?

// 1. Значення за замовчуванням: Якщо дані не розпарсилися, ми підставляємо порожній об'єкт 
// або масив, щоб програма не зламалася далі.

// let users = [];
// try {
//   users = JSON.parse(dataFromServer);
// } catch {
//   users = [];    // Якщо сервер прислав "сміття", працюємо з порожнім списком
// }
// 2. Сповіщення користувача: Вивести гарне віконце "Вибачте, технічні проблеми, спробуйте пізніше".

// Не намагайся загорнути весь код програми в один гігантський try...catch. 
// Це поганий тон. Загортай тільки ті місця, де помилка справді може статися 
// (як-от парсинг даних від сервера).


// Мікро-завдання для перевірки:
// Уяви, що ти отримуєш з сервера рядок, який іноді приходить як undefined (це не JSON!).

// const serverResponse = undefined; 
// console.log(JSON.stringify(serverResponse));  // undefined
// console.log(JSON.parse(serverResponse));  // Uncaught SyntaxError: "undefined" is not valid JSON

// try {
//    const result = JSON.parse(serverResponse);  // тут result = SyntaxError
//    console.log("Успіх:", result);  // ігнорується
// } catch (e) {
//    console.log(e.name + ":" + e.message);  // SyntaxError:"undefined" is not valid JSON
// }



// Іноді JSON приходить ідеальний з точки зору синтаксису (всі коми на місцях), але з точки зору 
// логіки він "битий". Наприклад, ти чекаєш об'єкт користувача, а прийшов порожній об'єкт {}.
// Ти можеш сама "викинути" помилку, щоб вона потрапила в блок catch. Це робиться за допомогою 
// команди throw.

// const rawData = '{}';    // Валідний JSON, але даних немає

// try {
//     const user = JSON.parse(rawData);   // пустий обєкт

//     if (!user.name) {   // якщо нема властивості name
//         // Ми самі створюємо помилку, бо дані нам не підходять
//         throw new Error("Дані неповні: відсутнє ім'я");
//     }

//     console.log(user.name);  // інакше виводимо name 
// } catch (error) {
//     console.log("Логічна помилка або збій парсингу: " + error.message);
// }


// написати свій фільтр, який, наприклад, видаляє всі властивості, 
// де значення — це занадто довгий рядок (більше 10 символів)?





                    // JSON.parse() and JSON.stringify() parameters

// 1. JSON.stringify(value, replacer, space)
// Цей метод перетворює JS-об'єкт у рядок.
// Параметр 1: value (обов'язковий)
//     Це дані, які ти хочеш запакувати (об'єкт, масив, число тощо).
// Параметр 2: replacer (опціональний)
// Це "фільтр" або "трансформатор". Може бути масивом або функцією.
// 2.1 Масив: вказуємо тільки ті ключі, які хочемо бачити в результаті.

// const user = { id: 1, name: "Чомучка", email: "test@ukr.net" };
// const json = JSON.stringify(user, ["name", "email"]); 
// // "{"name":"Чомучка","email":"test@ukr.net"}" (id зник)

// 2.2 Функція: дозволяє гнучко змінювати значення.
// const json = JSON.stringify(user, (key, value) => {
//   if (key === "email") return undefined; // видаляємо поле
//   if (typeof value === "string") return value.toUpperCase(); // текст у верхній регістр
//   return value; // повертаємо решту як є
// });
// "{"ID":1,"NAME":"ЧОМУЧКА"}"

// Параметр 3: space (опціональний)
// Робить JSON красивим (додає відступи).
// Число: кількість пробілів для відступу (макс. 10).
// Рядок: символ, який буде відступом (наприклад, "\t" для табуляції).
// JSON.stringify(user, null, 2); // Гарний об'єкт з відступом у 2 пробіли


// 2. JSON.parse(text, reviver)
// Цей метод перетворює рядок назад у JS-об'єкт.
// Параметр 1: text (обов'язковий)
//     Рядок, який ми парсимо. Має бути валідним JSON, інакше — SyntaxError.
    
// Параметр 2: reviver (опціональний)
// Це функція-"оживлятор". Вона дозволяє змінити значення прямо під час того,
//  як JSON перетворюється на об'єкт. 
//  Найголовніший кейс — Дати. JSON не вміє зберігати дати як об'єкти, 
//  він робить з них рядки. reviver повертає їм життя.
 
// const userData = '{"name": "Ivan", "registered": "2026-03-12T10:00:00Z"}';

// const user = JSON.parse(userData, (key, value) => {
//   // Якщо рядок схожий на дату — робимо з нього об'єкт Date
//   if (key === "registered") {
//     return new Date(value);
//   }
//   return value;
// });

// console.log(typeof user.registered); // "object" (це тепер справжня Date!)
// console.log(user.registered.getFullYear()); // 2026

// Коротке резюме:
// stringify(дані, фільтр, відступи) 
// - дані які ми хочемо запакувати
// - Фільтр: приховує секрети або змінює типи. 
// - Відступи: для читабельності.

// parse(текст, оживлятор)
// - Текст: дані які ми хочемо розпакувати
// - Оживлятор: перетворює рядки назад у складні об'єкти (наприклад, Date).



// Завдання :
// у тебе є об'єкт з цінами. Напиши JSON.stringify так, щоб він збільшив всі 
// ціни на 10% прямо під час перетворення в рядок.

// const productPrices = {
//     apples: 10,
//     grapes: 25,
//     bananas: 40,
//     oranges: 37,
//     pineapples: null
// }

// const productPricesJson = JSON.stringify(productPrices, (key, value) => {
//     if (typeof value === 'number') {
//         return value + (value * 10 / 100); // якщо знач.число то збільшити його на 10% і повернути
//     }
//     return value;  // всі інші значення поврнути в чистому вигляді
// }, 2)

// console.log(productPricesJson);


// Завдання:
// Ситуація
// Твій сервер прислав тобі дані про замовлення в інтернет-магазині. Але є проблема:
// Дати прийшли як звичайні рядки.
// Ціни прийшли як рядки з валютою (наприклад, "150$"), а тобі для розрахунків потрібні чисті числа.
// Твоє завдання
// Напиши JSON.parse з функцією reviver, яка:
// Перетворить поле date на справжній об'єкт new Date().
// Перетворить поле price, прибравши знак $ і зробивши його числом (підказка: можна використати 
//     .replace('$', '') та Number()).
// Всі інші поля залишити без змін.

// const orderJson = '{"id": 505, "date": "2026-03-13T12:00:00Z", "price": "150$", "status": "shipped"}';

// const order = JSON.parse(orderJson, (key, value) => {
//     if (key === 'date') {
//         return new Date(value);
//     }
//     if (key === 'price') {
//         return Number(value.replace('$', ''));
//     }
//     return value;
// })

// console.dir(order);
// console.log(`${order.date.getDate().toString().padStart(2, '0')}.${(order.date.getMonth() + 1.).toString().padStart(2, '0')}.${order.date.getFullYear()}`);

// ${order.date.getMonth() + 1} - тому що місяці в JS рахуються як індекси, від 0. Тобто січень це 0.
// тому треба +1 щоб отримати поточний місяць (як в реальності). 

// toString().padStart(2, '0') - зробить день і місяць двозначними числами, якщо число одинарне воно доповнить
//  його 0 спереду щоб було 2 числа 




// ---------------------------------------------------------------------------------------------------------------------------------------



                            // Callback. Callback hell



// callback hell
// http://it-notes.wiki/javascript/callbacks-basics-in-javascript/

// Колбек - це функція зворотнього виклику. Функція яка передається в іншу функцію як парамер.
// Колбек — це просто «спосіб використання» функції. А от коли вона виконається — залежить від того, хто її викликає.

// Часто колбеки пов'язують з асинхронністю (бо чекає в черзі свого виконання), 
// але не всі колбеки асинхронні. Наприклад методи масивів map, forEach...
// Тобто колбеки можуть бути як асинхронними так і синхронними.  

// 1. Синхронні колбеки (Synchronous Callbacks)
// Вони виконуються негайно, прямо під час виконання основної функції. Код чекає, поки колбек завершиться, 
// і тільки потім іде далі.

// Найкращий приклад — методи масивів (forEach, map, filter):

// console.log("Початок");

// const numbers = [1, 2, 3];
// numbers.forEach((num) => {
//     console.log("Число:", num); // Це синхронний колбек
// });

// console.log("Кінець");

// Результат у консолі:
// Початок
// Число: 1
// Число: 2
// Число: 3
// Кінець

// «Кінець» не надрукується, поки forEach не пройдеться по всіх числах. Це блокуючий код.

// 2. Асинхронні колбеки (Asynchronous Callbacks)
// Вони виконуються пізніше, після того, як завершиться основний потік коду. 
// Вони чекають на якусь подію (таймер, відповідь сервера, клік мишки).

// Приклад — setTimeout або addEventListener:

// console.log("1. Я замовив піцу");

// setTimeout(() => {
//     console.log("3. Піца приїхала! (колбек)");
// }, 2000);

// console.log("2. Я поки дивлюсь серіал");

// Результат у консолі:
// Я замовив піцу
// Я поки дивлюсь серіал
// Піца приїхала! (через 2 секунди)

// Тут setTimeout — це функція Web API. Вона прийняла колбек, засікла час і відпустила JavaScript далі. 
// Колбек виконається лише тоді, коли "пропікає" таймер.

// Як їх розрізнити?
// Головне правило: Дивись на "господаря" функції.

// JS-методи (Array, String, Object): Майже завжди синхронні. Вони просто допомагають обробити дані, які вже є в пам'яті.

// Web API (fetch, setTimeout, addEventListener): Майже завжди асинхронні. Вони працюють із зовнішнім світом, 
// де треба чогось чекати.



// Чому ми перейшли від асинхронних колбеків до Промісів?
// Раніше, щоб зробити три запити в інтернет один за одним, нам доводилося вкладати колбек у колбек. 
// Це називали Callback Hell (пекло колбеків):

// // ЖАХЛИВИЙ КОД:
// getData((user) => {
//     getPosts(user.id, (posts) => {
//         getComments(posts[0].id, (comments) => {
//             console.log(comments);
//         });
//     });
// });
// Проміси (і твій улюблений fetch) дозволили перетворити цю "драбину" на красивий рівний ланцюжок .then().then().then().

// Проміс дозволяє вибудувати код "в лінію", а не "вглиб".
// Раніше (Колбеки): "Зроби це, а КОЛИ закінчиш — зроби це всередині, а КОЛИ і те закінчиш — зроби ще це". 
// (Код росте вправо, стає схожим на трикутник).
// Зараз (Fetch + Promise): "Зроби це. ПОТІМ зроби це. ПОТІМ зроби це". (Код росте вниз, його легко читати).

// Підсумок:
// Колбек — це лише спосіб передачі функції.
// Синхронний колбек: виконується "тут і зараз" (як у forEach).
// Асинхронний колбек: виконується "колись потім" (як у setTimeout або після fetch).




                            // обєкт Promise та Fetch WebAPI


// https://uk.javascript.info/promise-basics
// https://uk.javascript.info/callbacks#pekelna-piramida
// https://uk.javascript.info/async

// https://uk.javascript.info/promise-chaining#skladnishii-priklad-fetch
// https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch


// Promise — це об’єкт JavaScript, який пов’язує код, що створюється, і код, який споживається.
// Promise - це об'єкт, який представляє результат асинхронної операції, який буде відомий колись у майбутньому

// Promise - це інших спосіб реалізації асинхронності (перший це setTimeout та setInterval)

// Promise - це спеціального виду обєкт, він вміє перекладати відповідальність за дію на WebAPI
// і просити виконати речі асинхронно. Різниця в тому що асинхронний колбек
// треба зробитти відкладено але ми не знаємо через скільки саме часу - тоді викор. promise
// як тільки проміс буде виконано тоді виконай дію (обєкт проміс містить прапорці для відстеження).


// fetch() — це сучасний вбудований у браузер метод для здійснення мережевих запитів (наприклад, 
// щоб отримати дані з сервера).
// fetch — це функція (інструмент), а Promise — це результат, який ця функція тобі видає.
// Коли ти викликаєш fetch(), він завжди повертає об'єкт типу Promise. Тобто fetch — це "фабрика", яка виробляє проміси.

// 1. fetch — це Web API (Інструмент браузера)
// fetch, як і setTimeout, setInterval або document.querySelector, — це не частина специфікації 
// мови ECMAScript (тобто самого JavaScript).
// Це інструменти, які браузер (або середовище Node.js) надає JavaScript-двигуну, щоб той міг взаємодіяти із 
// зовнішнім світом (мережею, таймерами, вікнами).

// 2. Promise — це об'єкт JS (Конструктор мови)
// Promise — це вбудований об'єкт самої мови JavaScript (з'явився в стандарті ES6).
// JavaScript-двигун "з коробки" знає, що таке проміс, як його створити і як з ним працювати.
// Це стандарт, за яким ми домовляємося обробляти будь-які асинхронні події.

// Це пояснює, чому fetch повертає саме Promise. Це як співпраця двох сторін:
// Браузер (Web API) каже: "Я пішов у мережу за твоїми даними через fetch. Це займе час".
// JavaScript каже: "Добре, я не буду чекати і стояти на місці. Дай мені Promise (об'єкт мого 
// стандарту), і коли ти повернешся з даними, ти просто 'закриєш' цей проміс результатом".

// setInterval (cb, 1000) - виконуй колбек кожну секунду
// button.addEventListener('click', ()=>{}) - виконай колюек коли відбудеться клік на button
// fetch('http://localhost:5000').then(()=>{}); - виконай колбек коли завантажаться дані


// fetch('http://localhost:5000') //- функція для завантаж даних (посилання це ресурс звідки завантажувати)
// завантаження даних не можже бути блокуючою операцією (не може зупиняти код) - їх ніколи ннен роблять синхронними

// const data = fetch('https://localhost:5000') - так не робити, це заблокує код на час поки дані не завантажаться

// fetch має метод then який вказує на колбек який треба виконати коли завантажаться дані :
// fetch('http://localhost:5000').then(()=>{}); // працює за рахунок того що під капотом використ проміси


// Реальний приклад:

// const weatherUrl = 'https://api.open-meteo.com/v1/forecast?latitude=48.6242&longitude=22.2947&current=apparent_temperature,is_day,rain,wind_speed_10m&timezone=GMT';
// fetch(weatherUrl).then((response) => {console.log(response);})
// коли виконається fetch (успішно!) тоді виконається КБ в методі then
// в консолі ми побачимо обєкт(результат промісу) типу даних ReadableStream (бінарна с-сма не читається людиною)
// і прийде в response - цей обєкт містить купу всього в т.ч. метод json()
// щоб могти прочитати дані треба його перетворити в JSON

// fetch(weatherUrl).then((response) => {return response.json();});
// метод .json() — це метод обєкту Response (Web API), і він теж повертає Promise.
// response.json() - це теж асинхронна операція
// те що повертається з попереднього колбека це теж проміс який віддає обєкт Response який потрапляє в наступний then,
// так утворюються ланцюжки викликів

// fetch(weatherUrl).then((response) => {return response.json();}).then(data => {console.log(data);})

// коли ми працюємо з синхронними фунцкіями, то результат зазвичай (те що повертає функція) приходить в змінну
// коли ми працєюємо з промісами то результат приходить як параметр в КБ-функцію наступного методу then
// then - це метод обєкта Promise

// Ми пишемо їх в рядочок тому що fetch повертає Promise і ми чіпляємося до Promise його ж методом then

// fetch — це локомотив, який тягне за собою вагончики-проміси, а .then() — це двері в ці вагончики.

// Як дізнатись чи Проміс закінчився чи ні?  Для цього у нього є стани(властивості)!
// якщо fetch(weatherUrl) прописати прямо в консолі, тот побачимо Promise (<pending>)
// стан Pending  - означає що він ще не виконався (в процесі) тобто then ще не виконується
// стан fulfilled - успішно завершився (тоді результат виконання потрапляє в [PromiseResult]),
//  який і є обєктом Response(webAPI) для першого then.
// наступний fetch(weatherUrl).then((response) => {return response.json();}) повертає теж проміс
// Response якого є data для наступного then 

// Стани Promise (властивості):
// - pending  - в процесі  (result)
// - fulfilled - завершився успішно (payload)
// - rejected - поламаний, помилка (error)



// Покрокова анатомія fetch:

// fetch повертає проміс МИТТЄВО, ще до того, як перший байт даних вилетів із сервера.

// Крок 1: Постріл (Виклик fetch)
// Ти пишеш const p = fetch('url').
// Браузер (Web API) каже: "Окей, я пішов у мережу!".
// JavaScript-двигун не чекає ні секунди. Він створює об'єкт Promise у стані pending (очікування) 
// і віддає його тобі в змінну p.
// Результат: У тебе вже є об'єкт, але він "порожній".

// Крок 2: Реєстрація (Метод .then)
// Ти пишеш p.then(callback).
// Ти кажеш цьому порожньому об'єкту: "Слухай, я всередину тебе покладу функцію (callback). 
// Коли ти 'наповнишся' результатом, будь ласка, виконай її".
// Результат: JavaScript іде далі виконувати інший код (наприклад, анімацію), він не заблокований.

// Крок 3: Очікування (Мережевий запит)
// Поки JS займається своїми справами, браузер десь там у фоні "спілкується" з сервером.
// Стан: Проміс усе ще pending.

// Крок 4: Момент істини (Resolve)
// Сервер відповів! Браузер отримав перші дані (заголовки відповіді).
// Браузер "стукає" в наш об'єкт Проміс і каже: "Гей, я приніс результат! Змінюй свій стан на fulfilled (виконано)".
// Що стається з результатом? Він "впорскується" всередину Проміса.

// Крок 5: Виклик (Execution)
// Як тільки стан Проміса змінився на "виконано", він автоматично згадує про функцію, 
// яку ми залишили в .then(), і викликає її, передаючи туди той самий результат (об'єкт Response).

// ⚡️ Важливе уточнення: Чи "забирається" проміс?
// Ні, проміс не зникає. Коли результат готовий, проміс просто "закривається".
// Уяви, що Проміс — це сейф.
// Спочатку тобі дають зачинений порожній сейф (pending).
// Ти кажеш: "Коли тут щось з'явиться — маякніть мені".
// Коли дані прийшли, сейф наповнюється і "замикається" назавжди з цим результатом.
// Проміс не віддає результат і не зникає. Він стає результатом. Ти не можеш "витягти" дані з 
// проміса в звичайну змінну просто так, ти можеш працювати з ними тільки всередині .then().

// Давай перевіримо це коротким кодом:

// console.log("1. Я стріляю fetch");
// const p = fetch('https://jsonplaceholder.typicode.com/todos/1');
// console.log("2. Ось що повернув fetch СРАЗУ:", p); 
// // Ти побачиш: Promise { <pending> }

// p.then(res => {
//     console.log("4. А ось тепер проміс наповнився і виконав цей код!");
// });

// console.log("3. Я виконуюся ПЕРЕД тим, як прийде відповідь");
// Порядок у консолі буде такий: 1 -> 2 -> 3 -> 4.
// Пункт 3 виконався раніше, ніж прийшла відповідь (пункт 4), хоча в коді він нижче!

// Обєкт Response — це окремий об'єкт, який ніяк не є Промісом.

// Уяви це як кур'єра та посилку:
// Проміс — це кур'єр. Він обіцяє принести щось.
// Response — це коробка (посилка), яку кур'єр тримає в руках, коли приходить до тебе.

// Як це працює в деталях:
// Проміс — це "менеджер" стану. Його робота — знати: "ми ще чекаємо" чи "вже все готово".
// Response — це об'єкт Web API, який містить саму відповідь сервера (статус 200, заголовки, тіло файлу).

// Коли Проміс переходить у стан Fulfilled, він не "стає" об'єктом Response. 
// Він просто передає цей об'єкт Response у функцію, яку ти написала всередині .then() як аргумент і викликає цю функцію.

// Давай подивимось на твій код очима JavaScript:
// fetch('url') // 1. Створили Проміс (Кур'єра)
//   .then(function(res) { // 2. "res" — це і є наш об'єкт Response (Посилка)
//       console.log(res instanceof Promise);  // false (Це не проміс!)
//       console.log(res instanceof Response); // true  (Це посилка!)
//   });
// Чому це важливо?
// Тому що у об'єкта Response свої власні методи, яких немає у Проміса:
// res.ok — чи все добре (статус 200-299).
// res.status — точний код (наприклад, 404).
// res.json() — метод, щоб почати читати вміст "посилки".

// Підсумуємо:
// Проміс — це лише обгортка, механізм очікування.
// Response — це вміст, те, що прилетіло по мережі.

// Коли ти пишеш .then(res => ...), ти кажеш: "Коли Проміс виконається, візьми те, що він приніс (об'єкт Response), 
// назви його словом res і дай мені з ним попрацювати".

// Давай подивимось на "генеалогічне дерево" одного запиту:
// fetch() — стріляє і створює Проміс №1.
// Проміс №1 — виконується і приносить нам об'єкт Response.
// Ми беремо Response і викликаємо його метод .json().
// Метод .json() миттєво створює Проміс №2 (обіцянку розпакувати дані).
// Проміс №2 — виконується і нарешті приносить нам JS-об'єкт (ті самі дані).

// Як це виглядає в коді (з коментарями "хто є хто"):

// fetch('https://api.example.com/data') // Тут ми працюємо з Promise №1
//   .then(res => { 
//     // res — це Response. У нього Є метод .json()
//     console.log(typeof res.json); // "function"
    
//     return res.json(); // Повертаємо Promise №2
//   })
//   .then(data => {
//     // data — це вже розпакований об'єкт (не Проміс і не Response)
//     console.log(data); 
//   });

// data — це результат виконання Промісу №2.

// Ось як працює ця "магія передачі":

// 1. Золоте правило .then()
// Коли ти всередині одного .then() робиш return чогось, це «щось» передається в наступний .then().
// Якщо ти повертаєш звичайне число або об'єкт, наступний .then() отримає його миттєво.
// Але якщо ти повертаєш новий Проміс (як це робить res.json()), наступний .then() буде чекати, 
// поки цей проміс виконається, і отримає вже його результат.

// 2. Чому data — це об'єкт, а не Проміс?
// Давай простежимо за "руками" JavaScript:
// Перший .then(res => ...):
// Він отримав Response (нашу "посилку").
// Всередині ми пишемо return res.json().
// Метод .json() каже: «Я пішов розпаковувати, ось тобі мій Проміс (обіцянка), що я це зроблю».
// Результат: Перший .then повертає цей новий Проміс далі по ланцюжку.
// Між першим і другим .then():
// JavaScript бачить: «О, мені повернули Проміс. Я не буду викликати наступний .then(),
// поки цей Проміс не буде виконаний (fulfilled)».
// Він чекає, поки дані розпакуються.
// Другий .then(data => ...):
// Як тільки розпакування завершено, Проміс №2 "відкривається".
// Всередині нього лежить уже чистий JS-об'єкт (наприклад, {id: 1, title: "Купити хліб"}).
// JavaScript бере цей об'єкт і кладе його в аргумент data.

// 3. Це залежить від завдання
// У першому .then() ми мали завдання: «Отримати посилку і почати її розпаковувати». 
// Результат розпакування — це складний процес, тому нам дали Проміс.
// У другому .then() ми маємо завдання: «Взяти готові дані та вивести їх». Тут нам уже не 
// треба нікуди йти і нічого чекати, дані вже лежать у нас на столі у вигляді data.

// Давай підсумуємо ланцюжок "перетворень":
// fetch() ➔ Повертає Проміс.
// .then(res => ...) ➔ Отримує Response, повертає новий Проміс від .json().
// .then(data => ...) ➔ Отримує результат (чисті дані) і просто їх використовує.

// Важливо: Наступний .then() завжди отримує те, що було результатом виконання попереднього етапу. 
// Якщо попередній етап повернув Проміс — наступний отримає те, що було всередині цього Проміса.

// Підсумок для закріплення:
// Promise: Має методи .then(), .catch(), .finally(). Він — "менеджер очікування".
// Response: Має методи .json(), .text(), .blob() та властивості .ok, .status. Він — "вміст відповіді".

// Запам'ятай правило: Ми не можемо розпакувати "обіцянку" (Promise), ми можемо розпакувати тільки "відповідь" 
// (Response), яку ця обіцянка нам принесла.



// Error у fetch:
// Якщо посилання (ресурс) звідки ми хочемо отримати дані невірні, то викинеться помилка Error
// Що можна зробити щоб передбачити помилки і щоб не зламався код:
// - огорнути в try...catch
// - використати метод catch до fetch - це його спеціальний обробник на помилки (він додається в кінці ланцюжка)
// він працює як блок catch в конструкції try...catch але для fetch (і приймає колбек),
// який на будь якому етапі виконання перехоплює помилку
// fetch(weatherUrl)
//     .then((response) => {return response.json();})
//     .then(data => {console.log(data);})
//     .catch(err => console.log(err));



// then може приймати два параметра, перший на успіх (cb on fulfilled) дугий на помилку (cb on rejected)
//Альтернативна форма запису де catch іде другим параметром в отсанньому then:
// fetch(weatherUrl)
//     .then((response) => {return response.json();})
//     .then(data => {console.log(data);}, err => console.log(err))  // це замість catch для цього then
// підходить для специфічних операцій, якщо then потребує особливого підходу
// в інших випадках краще писати catch в ланцюжок викликів





// Приклад-практика : підвантаження погоди

// const weatherUrl1 = 'https://api.open-meteo.com/v1/forecast?latitude=48.6242&longitude=22.2947&current=apparent_temperature,is_day,rain,wind_speed_10m&timezone=GMT&wind_speed_unit=ms';
// const weatherUrl2 = 'https://api.open-meteo.com/v1/forecast?latitude=48.6242&longitude=22.2947&current=apparent_temperature,is_day,rain,wind_speed_10m&timezone=GMT&temperature_unit=fahrenheit';
// // у fetch схема роботи завжди приблизно однакова
// fetch(weatherUrl1)
//     .then((response) => response.json())  // розпарсити(десереалізувати) отриманий Response (у випадку resolved)
//     .then(data => createWeather(data))  // використати розпарсені(десеріалізовані) дані - передамо їх у функцію
//     .catch(err => console.log('error:', err));  // перехопити помилку у випадку rejected

// // отримані дані(розпарсений json) ми будем використовувати, тому виведемо їх на сторінку, для цього
// // відобразимо на сторінці поточну температуру з од.вим.
// // відобразити температуру відємну синім кольором, 0 чорним, додатні до 40 - зеленим, додатню вище 40 - червоним

// // згенеруємо розмітку динамічно

// // ця функція викликається в fetch then де ми отримали розпарсені дані,я кі передаємо зразу в цю функцію,
// //  інакше ми не зможемо отримати дані з обєкту data - нема як їх взяти
// function createWeather({
//     // деструктруємо обєкт data + деструктуруємо кожну потрібну властивість щоб отримати їх apparent_temperature та wind_speed
//     // і щоб не було по дві однакові назви,останні apparent_temperature та wind_speed перейменовуємо в tempUnit та windSpeedUnit 
//     current:{apparent_temperature, wind_speed_10m},
//     current_units:{apparent_temperature:tempUnit, wind_speed_10m: windSpeedUnit}
// }) {   
//     // apparent_temperature = -40;  // перевизначаємо знач темп щоб протестити розгалуження у функції calcTemperatureColor
//     const currentTemperatureEl = document.createElement('div');
//     currentTemperatureEl.textContent = `${apparent_temperature} ${tempUnit}`;
//     currentTemperatureEl.style.color = calcTemperatureColor(apparent_temperature);

//     const currentWindSpeedEl = document.createElement('div');
//     currentWindSpeedEl.textContent = `${wind_speed_10m} ${windSpeedUnit}`;

//     document.body.append(currentTemperatureEl, currentWindSpeedEl);
// }

// function calcTemperatureColor (temperature) {
//     if (temperature < 0) {
//         return 'blue';
//     } else if (temperature === 0) {
//         return 'black';
//     } else if (temperature > 0 && temperature < 40) {
//         return 'green';
//     } else {
//         return 'red';
//     }
// }



// Приклад-практика : підвантаження погоди ->  Кнопка перемикач + рефакторинг

// прапорець, від нього залежить одиниця виміру температури - єдине джерело істини
// let isCelsiiDegree = true;

// // отримуємо кнопку з html
// const temperatureBtn = document.querySelector('#temperatureUnitBtn');
// // рендерим одразу на сторінку дані в залежності від стану isCelsiiDegree
// updateData();
// // навішуємо оброник на клік на кнопку-перемикач
// temperatureBtn.onclick = switchTemperatureUnit;
// //обробник : 
// function switchTemperatureUnit() {
//     // поміняти знач прапорця на протилежний
//     isCelsiiDegree = !isCelsiiDegree;
//     // перерендерити сторінку бо відбувся клік на кнопку-перемикач (isCelsiiDegree = false)
//     updateData();
// }

// // функція для перерендера
// function updateData() {
//     // кнопка-перемикач - змінює текст в залежності від isCelsiiDegree
//     // якщо зараз цельсій то на кнопкі напис перемкнутись на Фаренгейт,
//     // якщо зарах Фаренгейт то на кнопкі написа перемкнутись на цельсій
//     temperatureBtn.textContent = `Switch to ${isCelsiiDegree ? 'F' : 'C'}`;

//     // перше посилання на цельсій, другий на фаренгейт
//     const weatherUrl1 = 'https://api.open-meteo.com/v1/forecast?latitude=48.6242&longitude=22.2947&current=apparent_temperature,is_day,rain,wind_speed_10m&timezone=GMT&wind_speed_unit=ms';
//     const weatherUrl2 = 'https://api.open-meteo.com/v1/forecast?latitude=48.6242&longitude=22.2947&current=apparent_temperature,is_day,rain,wind_speed_10m&timezone=GMT&wind_speed_unit=ms&temperature_unit=fahrenheit';

//     // при першому завантаженні сторінки і при настисканні кнопки перемикача отримуєм 
//     // свіжі дані за вказаними посиланнями (у fetch схема роботи завжди приблизно однакова)
//     fetch(`${isCelsiiDegree ? weatherUrl1 : weatherUrl2}`)
//         .then((response) => response.json())  // розпарсити(десереалізувати) отриманий Response (у випадку resolved)
//         .then(data => createWeather(data))  // використати розпарсені(десеріалізовані) дані - передамо їх у функцію
//         .catch(err => console.log('error:', err));  // перехопити помилку у випадку rejected
    
//     // отримані дані(розпарсений json) ми будем використовувати, через фукцію createWeather
//     // відобразити температуру відємну синім кольором, 0 чорним, додатні до 40 - зеленим, додатню вище 40 - червоним
// }

// // згенеруємо розмітку в html а дані (контент) динамічно

// // ця функція викликається в fetch then де ми отримали розпарсені дані,які передаємо зразу в цю функцію,
// //  інакше ми не зможемо отримати дані з обєкту data - нема як їх взяти
// function createWeather({
//     // деструктруємо обєкт data + деструктуруємо кожну потрібну властивість щоб отримати їх apparent_temperature та wind_speed
//     // і щоб не було по дві однакові назви,останні apparent_temperature та wind_speed перейменовуємо в tempUnit та windSpeedUnit 
//     current:{apparent_temperature, wind_speed_10m},
//     current_units:{apparent_temperature:tempUnit, wind_speed_10m: windSpeedUnit}
// }) {
//     // отримую елемент div html
//     const currentTemperatureEl = document.querySelector('div.temperature');
//     // встановлюю контент з розпарсеного обєкта data
//     currentTemperatureEl.textContent = `${apparent_temperature} ${tempUnit}`;
//     // викликаю функцію задання кольору в залежності від градусів
//     currentTemperatureEl.style.color = calcTemperatureColor(apparent_temperature);
    
//     // отримую елемент div html
//     const currentWindSpeedEl = document.querySelector('div.windSpeed');
//     // встановлюю контент з розпарсеного обєкта data
//     currentWindSpeedEl.textContent = `${wind_speed_10m} ${windSpeedUnit}`;

// }

// // функція що викликається з фунцкії createWeather для задання кольору в залежності від градусів
// function calcTemperatureColor (temperature) {
//     if (temperature < 0) {
//         return 'blue';
//     } else if (temperature === 0) {
//         return 'black';
//     } else if (temperature > 0 && temperature < 40) {
//         return 'green';
//     } else {
//         return 'red';
//     }
// }



// Приклад-практика : підвантаження погоди -> Кнопка перемикач + changeColor for F

// let isCelsiiDegree = true;
// const tempUnitBtn = document.querySelector('#temperatureUnitBtn');
// tempUnitBtn.onclick = switchTemperature;
// updateData();

// function switchTemperature () {
//     isCelsiiDegree = !isCelsiiDegree;
//     updateData();
// }


// function updateData() {
//     tempUnitBtn.textContent = `Switch to ${isCelsiiDegree ? 'F' : 'C'}`;

//     const weatherUrl1 = 'https://api.open-meteo.com/v1/forecast?latitude=48.6242&longitude=22.2947&current=apparent_temperature,is_day,rain,wind_speed_10m&timezone=GMT&wind_speed_unit=ms';
//     const weatherUrl2 = 'https://api.open-meteo.com/v1/forecast?latitude=48.6242&longitude=22.2947&current=apparent_temperature,is_day,rain,wind_speed_10m&timezone=GMT&wind_speed_unit=ms&temperature_unit=fahrenheit';

//     fetch(isCelsiiDegree ? weatherUrl1 : weatherUrl2)
//         .then(response => response.json())
//         .then(data => createWeather(data))
//         .catch(error => console.log(`Error: ${error.message}`));
// }

// function createWeather({
//     current:{apparent_temperature, wind_speed_10m},
//     current_units:{apparent_temperature:tempUnit, wind_speed_10m: windSpeedUnit}
// }) {

//     const temperatureEl = document.querySelector('div.temperature');
//     temperatureEl.textContent = `${apparent_temperature} ${tempUnit}`;
//     temperatureEl.style.color = calcTemperatureColor(apparent_temperature);

//     const windSpeedEl = document.querySelector('div.windSpeed');
//     windSpeedEl.textContent = `${wind_speed_10m} ${windSpeedUnit}`;

// }

// function calcTemperatureColor(temperature) {
//     return isCelsiiDegree ? calcTemperatureColorC(temperature) : calcTemperatureColorF(temperature);
// }

// function calcTemperatureColorC(temperature) {
//     if (temperature < 0) {
//         return 'blue';
//     } else if (temperature === 0) {
//         return 'black';
//     } else if (temperature > 0 && temperature <= 40) {
//         return 'green';
//     } else {
//         return 'red';
//     }
// }

// // C перевести у F :  C * 2  + 30
// // F перевести у C: (F - 30) / 2
// function calcTemperatureColorF(temperature) {
//     if (temperature < 30) {
//         return 'blue';
//     } else if (temperature === 30) {
//         return 'black';
//     } else if (temperature > 30 && temperature <= 110) {
//         return 'green';
//     } else {
//         return 'red';
//     }
// }


// Приклад-практика : підвантаження погоди -> Кнопка перемикач + changeColor for F + SwitchCase

// let isCelsiiDegree = true;

// const tempUnitBtn = document.querySelector('#temperatureUnitBtn');

// updateData();

// tempUnitBtn.onclick = switchDegree;

// function switchDegree() {
//     isCelsiiDegree = !isCelsiiDegree;
//     updateData();
// }

// function updateData() {
//     tempUnitBtn.textContent = `Swicth to ${isCelsiiDegree ? 'F' : 'C'}`;

//     const weatherUrl1 = 'https://api.open-meteo.com/v1/forecast?latitude=48.6242&longitude=22.2947&current=apparent_temperature,is_day,rain,wind_speed_10m&timezone=GMT&wind_speed_unit=ms';
//     const weatherUrl2 = 'https://api.open-meteo.com/v1/forecast?latitude=48.6242&longitude=22.2947&current=apparent_temperature,is_day,rain,wind_speed_10m&timezone=GMT&wind_speed_unit=ms&temperature_unit=fahrenheit';

//     fetch(isCelsiiDegree ? weatherUrl1 : weatherUrl2)
//         .then(response => response.json())
//         .then(data => createWeather(data))
//         .catch(error => console.log(`Error: ${error.message}`));

// }

// function createWeather({
//     current:{apparent_temperature, wind_speed_10m},
//     current_units:{apparent_temperature: temperatureUnit, wind_speed_10m: windSpeedUnit}
// }) {

//     const temperatureEl = document.querySelector('div.temperature');
//     temperatureEl.textContent = `${apparent_temperature} ${temperatureUnit}`;
//     temperatureEl.style.color = calcTempColor(apparent_temperature);

//     const windSpeedEl = document.querySelector('div.windSpeed');
//     windSpeedEl.textContent = `${wind_speed_10m} ${windSpeedUnit}`;
// }

// function calcTempColor(temperature) {
//     const scaleDegree = isCelsiiDegree ? {cold: 0, hot: 40} : {cold: 30, hot: 110};

//     switch(true) {
//         case (temperature < scaleDegree.cold) : 
//             return 'blue';
//         case (temperature === scaleDegree.cold) :
//             return 'black';
//         case (temperature > scaleDegree.cold && temperature < scaleDegree.hot):
//             return 'green';
//         default: 
//             return 'red';
//     } 
// }




                    // new Promise() - Промісифікація



// Promise - це обєкт-конструктор в JS (V8)
// setTimeout, setInterval, fetch - це функції WebAPI, в яких під капотом використ. контсруктор Promise

// Promise і використати його API

// Якщо ми хочемо самостійно створити Promise то ми можемо зробити це через спеціальний конструктор
// new Promise

// const myPromise = new Promise(function(resolve, reject) {
//     // код що поверне результат
// });

// Promise має три стани : pending, fullfiled, rejected (як у fetch)
// Але якщо ми пишемо Promise самі, нам потрібно визначити як привести його в стан fullfilled або rejected
// для цього нам надають наступний спосіб : Promise повинен мати callback - це функція-виконавець
// Цей callback-виконавець теж повинен мати два callback : 1 на fullfiled(resolved), 2 на rejected(reject)
// Щоб просигналізувати що все завершилось успіхом - викликаємо resolve
// Щоб повідомити що все завершилось невдачею - викликаємо reject

// function promiseCB (resolve, reject) {
//     // тут відбуваються якісь дії
//     // resolve('success result');

//     reject(new Error('something went wrong'));
// }

// const promise = new Promise (promiseCB);

// promise.then(data => console.log(data)).catch(e => console.log(e));

// ось ці параметри в promiseCB це є статичні методи Promise. Тобто ми приймаємо як параетри і 
// викликаємо потрібний МЕТОД Promise.
// Для перевірок чи тестування до них можна звертатись напряму через сам конструктор і перехопити його за
//  допомогою then(resolve) або catch(reject):
// Promise.resolve([{user: 'Test'}]).then(data => {
//     console.log(data);
// })
// Promise.reject().catch();


// Статичні методи Promise та методи інстанса  - почитати!!!!!!!!!!!!!!!!!

// проміс - кіт Шредінгера
// Завдання створити свій проміс

// 2 прописуємо обробник проміса, який приймає два колбека - методи обєкта Promise
// const executor = function (resolve, reject) {
//     // рандомно обираючи число обробляємо на успіх або неуспіх відповідно і викликаємо методи Promise
//     if (Math.random() < 0.5) {
//         resolve('cat is alive');
//     } else {
//         reject(new Error('cat is not alive'))
//     }
// }
// const shredCat = new Promise(executor);  // 1 створюємо проміс ч/з конструктор і передаємо callback - executor (обробник проміса)

// // 3 Обробляємо отриманий результат за допомогою then або catch
// shredCat.then(data => console.log(data)).catch(e => console.log(e))


// Промісифікація - це перероблення чогось таким чином щоб працювати з ним на основі синтаксиса Promise

// Завдання
// з setTimeout(який працює на колбеках зробити схожий вигляд)
// setTimeout(cb, 1000)
// delay(1000).then(cb)

// 1 створ.функцію яка поверне проміс (як парам приймає час відкладки)
// function delay(ms) {

//     // 3 створ.функцію-виконавця/обробник який приймає як параметри методи обєкту Проміс
//     const executor1 = function(res, rej) {
//         // перевіримо на помилки перед тим як викликати колбек на успіх
//         if (typeof ms !== 'number') {
//             rej(new TypeError('ms must be a number'));
//         }
//         if (ms < 0 || !Number.isInteger(ms)) {
//             rej(new RangeError('ms must be a positive integer'));
//         }

//         // в середині функції-виконавця запускаємо setTimeout, яка виконає resolve через час,
//         // що приймає функція
//         setTimeout(res, ms);
//     }

//     // 2 сворюємо проміс і одразу його повертаємо для then (як параметр отримує функцію-виконавця/обробник)
//     return new Promise(executor1);
// }

// // 4 до функції що повертає проміс, застосовуємо метод Promise then для обробки успіху
// delay(1000).then(() => console.log('setTimeout resolved')).catch(e => console.log(e))


// Приклад завантаження картинки і рендер в синтаксисі проміса
// берем реальний лінк на картинку
// const src = 'https://dettext.com/wp-content/uploads/2024/02/400261-dettext_com-Mini-kartinki-dlia-srisovki-milye-i-legkie.jpg'

// // до фукції loadImage (яка повертатиме порміс) застосовуємо then...catch
// loadImage(src)
//     .then(img => {
//         // якщо resolved то додати картинку на сторінку
//         document.body.append(img);
//     })
//     // якщо rejected то викинути помилку
//     .catch(e => console.log(e));

// // функція loadImage яка завантажує картинку за посиланням і повертає Проміс для обробки в then.catch
// function loadImage(src) {
//     // створюємо інстанс проміса (як парам приймає методи resolve, reject)
//     return new Promise((res, rej) => {
//         // створюємо картинку
//         const img = document.createElement('img');
//         img.src = src;  // додаємо атрибут src з посиланням на картинку
//         // вішаємо на картинку обробника на завантаження (DOM WebAPI), який виконає колбек одразу
//         //  після завершення завантаження (тобто викличе метод resolve, якого перехопить thens і 
//         // додасть елемент на сторінку)
//         img.onload = () => {
//             res(img);
//         };
//         // вішаємо на картинку обробник на помилку під час завантаж(DOM WebAPI)б який виконає колбек
//         // у випадку виникнення помилки (тобто викличе метод reject, якого перехопить catch)
//         img.onerror = () => {
//             rej(new Error('Image was not loaded'));
//         };
//     });
// }


// Зазираємо в середину проміса і перевіряємо state та result:
// Створимо проміс, який виконається через 10 секунд
// const myObservedPromise = new Promise((res) => {
//     setTimeout(() => res({ message: "Я всередині!" }), 10000);
// });

// console.log("Ось він спочатку:", myObservedPromise);
// // [[PromiseState]]: "pending"
// // [[PromiseResult]]: undefined


// // Через 11 секунд подивимось на нього знову
// setTimeout(() => {
//     console.log("А ось він з даними:", myObservedPromise);
// }, 11000);
// // [[PromiseState]]: "fulfilled"
// // [[PromiseResult]]: Object
// // message: "Я всередині!"





// 🎮 Твоє завдання: "Лотерея"
// Уяви, що ти пишеш гру або сайт, де користувач має натиснути на кнопку. 
// Але ти хочеш зробити це через Promise, щоб потім зручно ланцюжком .then() 
// запустити наступну дію (наприклад, привітання).
// Напиши функцію runLottery(), яка імітує розіграш призу.
// Умови:
// Функція повинна повертати новий Promise.
// Всередині використовуй setTimeout, щоб імітувати "барабан, що крутиться" (нехай це триває 2 секунди).
// Після 2 секунд згенеруй випадкове число від 0 до 1 (ти вже це робила через Math.random()).
// Успіх (resolve): Якщо число більше ніж 0.5, викликай resolve з текстом: "Ви виграли приз! 🎁".
// Помилка (reject): Якщо число 0.5 або менше, викликай reject з текстом: "На жаль, спробуйте ще раз. 😢".
// Твій "шаблон" для заповнення:
// Спробуй дописати код усередині. Це якраз та сама промісифікація таймера з логікою вибору. 


// function runLottery() {
//     // повертаю новий проміс
//     return new Promise((resolve, reject) => {

//         // в середині проміса створюю setTimeout
//         setTimeout(()=> {
//             // обираю рандомне число між 0 та 1
//             const num = Math.random()
//             console.log(num);
//             // якщо число >0.5 викликаю resolve інакше reject
//             num > 0.5 ? resolve('Ви виграли приз! 🎁"') : reject('На жаль, спробуйте ще раз. 😢');
//             // все що в середині setTimeout виконається через 2 сек
//         }, 2000);
//         // за межами setTimeout не вийде зробити перевірку числа та викликати resolve або reject
//         // тому що це синхронний код, він виконається одразу ще до того як таймер простучить 2 сек
//         // і в num у нас буде undefined і відповідно завжди буде викликатись reject   

//     });
// }

// // А тут виклич функцію і оброби результат через .then() та .catch()
// runLottery()
//     .then(message => console.log(message))
//     .catch(error => console.error(error));





// 🔐 Завдання: "Імітація входу в систему"
// Тобі потрібно написати функцію loginUser(password), яка імітує перевірку пароля на сервері.
// Умови:
// Функція повертає новий Promise.
// Використовуй setTimeout на 1.5 секунди (імітуємо затримку мережі).
// Логіка всередині таймера:
// Якщо пароль дорівнює "admin123", викликай resolve з об'єктом: { id: 1, name: "Admin", role: "super-user" }.
// Якщо пароль будь-який інший, викликай reject з текстом: "Доступ заборонено! Невірний пароль.".
// На що звернути увагу:
// Пам'ятай: вся логіка if/else та виклики resolve/reject мають бути всередині колбеку setTimeout.

// Спробуй повернути в resolve саме об'єкт, а не рядок. Це покаже, як дані (наш [[PromiseResult]]) 
// можуть бути складною структурою.
// Твій шаблон:

// function loginUser(password) {
//     return new Promise((resolve, reject) => {
//         console.log("Перевірка даних на сервері...");

//         // ТУТ ТВІЙ КОД З ТАЙМЕРОМ
//         setTimeout(() => {

//             debugger;
//                 password === 'admin123' 
//                 ? resolve({ id: 1, name: "Admin", role: "super-user" }) 
//                 : reject('Доступ заборонено!');
//         }, 1500)
//     });
// }

// Перевірка (спробуй ввести правильний і неправильний пароль)
// loginUser("admin123")
//     .then(user => console.log("Ласкаво просимо,", user.name))
//     .catch(error => console.error(error));



// Завдання: "Система замовлень в інтернет-магазині"
// Тобі потрібно написати функцію getOrderDetails(orderId), яка імітує отримання даних про замовлення.
// Умови:
// Функція повертає новий Promise.
// Використовуй setTimeout на 2 секунди.
// Логіка всередині (використовуй замикання):
// У нас є "база даних" (просто змінна всередині або зовні): const status = "оплачено".
// Якщо orderId — це число більше за 0, викликай resolve з об'єктом, який містить:
// Переданий orderId.
// Назву товару (вигадай будь-яку).
// Статус (візьми зі змінної status через замикання).
// Якщо orderId менше або дорівнює 0 (або не число), викликай reject з текстом: 
// "Помилка: Невірний номер замовлення".
// На що звернути увагу:
// Замикання: Зверни увагу, як твій resolve спокійно візьме змінну status, хоча вона лежить
//  "поверхом вище" від setTimeout.
// Валідація: Це гарна звичка — завжди перевіряти вхідні дані (orderId) перед тим, як щось робити.

// Твій шаблон:

// function getOrderDetails(orderId) {
//     const status = "оплачено"; // Змінна в лексичному оточенні

//     return new Promise((resolve, reject) => {
//         console.log(`Пошук замовлення №${orderId}...`);

//         setTimeout(() => {

//             // 1 варіант if...else
//             // if (typeof orderId !== 'number' || orderId <= 0) {
//             //     reject(new Error('Помилка: Невірний номер замовлення'))
//             // } else {
//             //     resolve({id: orderId, productName: 'Cookies', status: status})
//             // }

//             // 2 варіант ternary operator
//             (typeof orderId !== 'number' || orderId <= 0)
//             ? reject(new Error('Помилка: Невірний номер замовлення'))
//             : resolve({id: orderId, productName: 'Cookies', status: status});

//         }, 2000);
//     });
// }

// // Перевірка
// getOrderDetails(105)
//     .then(order => console.log("Дані отримано:", order))
//     .catch(err => console.error(err));


// 📝 Завдання №1: "Генератор кухонних передбачень"
// Уяви, що ти робиш сайт-розвагу. Користувач натискає кнопку, і за 3 секунди отримує випадкове передбачення.
// Твоя задача:
// Напиши функцію getFortune(), яка:
// Не приймає ніяких параметрів.
// Повертає новий Promise.
// Всередині має setTimeout на 3 секунди.
// Логіка всередині таймера:
// Створи масив з 3-4 передбачень (рядків). 
// Наприклад: "На тебе чекає кава", "Сьогодні вдалий день для JS", "Купи собі шоколадку".
// Вибери випадкове передбачення з масиву (використовуй Math.random() та Math.floor()).
// Якщо передбачення успішно вибрано (воно існує) — викликай resolve і передай туди це передбачення.
// Якщо раптом щось пішло не так (наприклад, масив порожній) — викликай reject з 
// текстом "Магія не спрацювала".
// Важливо: Після опису функції, не забудь її викликати та обробити результат через .then() 
// та .catch(), щоб вивести результат у консоль.

// function getFortune() {
//     return new Promise((resolve, reject) => {
//         console.log('Очікуйте на магію...');

//         setTimeout(() => {
//             const predictions = ['Чудовий день для кави з поругою', 'Сьогодні просто необхідно побалувати себе', 'Забий на все та прочитай гарну книгу', 'Подбай про себе та відвідай SPA'];
//             const currPrediction = predictions[Math.floor(Math.random() * predictions.length)];
            
//             currPrediction 
//             ? resolve(currPrediction) 
//             : reject('Ой, здається сьогодні передбачень не передбачено))');

//         }, 3000);
//     }) 

// }

// getFortune()
//     .then(prediction => {console.log('Ваше передбачення на сьогодні: ' + prediction)})
//     .catch(error => {console.error(error)});




// Завдання №2: "Детектор солодкого"
// Це завдання трохи складніше, бо тут з'являється параметр, який ти маєш передати у функцію,
//  і на основі нього вирішити долю промісу.
// Умова:
// Напиши функцію checkSweetness(sugarGram).
// Функція приймає параметр sugarGram (кількість грамів цукру в десерті).
// Повертає новий Promise.
// Має затримку (setTimeout) на 2 секунди.
// Логіка всередині:
// Якщо sugarGram більше 50, проміс має виконатися з помилкою (reject). 
// Текст: "Обережно! Це занадто солодко для одного разу! 🍫🚫".
// Якщо sugarGram від 1 до 50, проміс виконується успішно (resolve). 
// Текст: "Смачного! Це ідеальна порція щастя. 🍰✅".
// Якщо передано 0 або від'ємне число, проміс теж має піти в reject. 
// Текст: "Хіба це десерт без цукру? 🧐".
// Твій виклик: Напиши весь код самa. Спробуй викликати функцію кілька разів з різними числами 
// (наприклад, 20, 100 і 0), щоб побачити, як спрацьовують різні гілки коду.

// function checksweetness(sugarGram) {
//     return new Promise((res, rej) => {
//         setTimeout(() => {
//                     if (sugarGram <= 0) {
//             rej('Хіба це десерт без цукру? 🧐');
//             return;
//         }
//         if (sugarGram > 50)  {
//             rej('Обережно! Це занадто солодко для одного разу! 🍫🚫');
//             return;
//         }
//         res('Смачного! Це ідеальна порція щастя. 🍰✅');
//         }, 2000)
//     })

// }

// checksweetness(98)
//     .then(message => console.log(message))
//     .catch(error => console.error(error));


// Завдання №3: "Завантажувач зображень" (Фінальний бос створення промісів)
// Давай перейдемо до чогось, що максимально схоже на реальну роботу браузера.
//  Уяви, що ти завантажуєш картинку.
// Умова:
// Напиши функцію loadImage(imageName).
// Функція приймає назву файлу (рядок).
// Повертає новий Promise.
// Затримка — 1.5 секунди.
// Логіка:
// Створи масив "дозволених" форматів: ['jpg', 'png', 'gif'].
// Тобі потрібно перевірити, чи закінчується imageName на один із цих форматів. 
// (Підказка: використай метод рядка .endsWith() або просто перевір останні 3 літери).
// Якщо формат правильний — resolve з текстом: "Зображення [назва] успішно завантажено! 🖼️".
// Якщо формат не підходить (наприклад, "video.mp4" або "doc.pdf") — reject з текстом:
//  "Помилка: Непідтримуваний формат файлу. ❌".
// Твій виклик: Напиши все з нуля. Спробуй перевірити функцію з правильним файлом 
// (наприклад, "cat.jpg") та неправильним (наприклад, "homework.zip").

// function loadImage(imageName) {
//     return new Promise((res, rej) => {
//         const suitableFormats = ['jpg', 'png', 'gif', 'jpeg'];
//         // split- розрізає стрінг по вказаному роздільнику і запаковує в масив, pop видаляє останній елемент і повертає його
//         // це універсальний спосіб отримати всі символи після крапки (тобто отримати розширення назви файлу)
//         const currImageFormat = imageName.split('.').pop(); 
//         setTimeout(() => {
//             if (!suitableFormats.includes(currImageFormat)) {
//                 rej('Помилка: Непідтримуваний формат файлу. ❌');
//                 return;
//             }
//             res(`Зображення ${imageName} успішно завантажено! 🖼️`)

//         }, 1500)
//     })
// }

// loadImage('dog.jpeg')
//     .then(message => console.log(message))
//     .catch(error => console.error(error));



// Завдання №4: "Промісифікація будильника"
// У нас є стара функція setOldAlarm, яка працює тільки через колбеки. 
// Вона вже написана (уяви це). Тобі треба написати нову функцію-обгортку.
// Твоя задача:
// Напиши функцію alarmPromise(time).
// Вона має повертати новий Promise.
// Всередині вона має використовувати звичайний setTimeout.
// Якщо time менше 0 — відразу reject("Час не може бути від'ємним").
// Через кількість мілісекунд, що дорівнює time, проміс має виконатися (resolve)
//  з текстом "Пора вставати! ⏰".


// function alarmPromise(time) {
//     return new Promise((res, rej) => {
//             if (time < 0 || !time) {
//                 rej("Час не може бути від'ємним або відсутнім");
//                 return;
//             }
//             setTimeout(() => {
//                 res('Пора вставати! ⏰');
//             }, time)

//     })
// }

// alarmPromise(2000)
//     .then(message => console.log(message))
//     .catch(err => console.error(err));


// Завдання №5: "Промісифікація завантаження скрипта"
// Це класична задача з реального життя. Уяви, що тобі треба додати на сторінку сторонній
//  скрипт (наприклад, плеєр YouTube або карту Google). У браузері для цього використовується 
//  об'єкт document.createElement('script').
// Твоя задача: Написати функцію loadScript(url).
// Умови:
// Функція повертає новий Promise.
// Всередині створюється елемент: const script = document.createElement('script');.
// Встановлюється адреса: script.src = url;.
// Важливо: У скриптів є дві події (колбеки), які нам треба "промісифікувати":
// script.onload — спрацьовує, якщо скрипт завантажився успішно. Тут треба викликати resolve.
// script.onerror — спрацьовує, якщо сталася помилка (наприклад, 404). Тут треба викликати reject.
// Після налаштування подій скрипт треба додати на сторінку: document.head.append(script);.
// Чому це важливо? Це реальна промісифікація "старих" подій браузера. Спробуй написати це сама.
// Підказка:
// JavaScript
// script.onload = () => resolve("Скрипт завантажено!");
// script.onerror = () => reject("Помилка завантаження");

// function loadScript(url) {
//     return new Promise((resolve, reject) => {
//         const script = document.createElement('script');
//         script.src = url;

//         script.onload = () => resolve('Скрипт завантажено!');
//         script.onerror = () => reject(new Error('Помилка завантаження'));

//         document.head.append(script);
//     })
// }

// loadScript('https://code.jquery.com/jquery-3.7.1.min.js')
//     .then(message => console.log(message))
//     .catch(error => console.error(error));




// Завдання №6: "Послідовні дії"
// Напиши функцію delay(ms), яка просто повертає проміс, що виконується через ms мілісекунд. 
// (Ми це вже робили, це просто "пауза").
// А тепер спробуй поєднати це:
// Завантаж картинку (використай свою функцію loadImage, яку ми писали раніше).
// ПІСЛЯ того, як картинка завантажиться (у .then), почекай 3 секунди (використай delay).
// І тільки ПІСЛЯ цієї паузи додай картинку в document.body.
// Підказка: щоб зробити паузу всередині .then, треба написати return delay(3000); і додати наступний .then.


// function loadImage(url) {

//     return new Promise((res, rej) => {

//         const img = new Image();
//         img.src = url;
    
//         img.onload = () => res(img);
//         img.onerror = () => rej(new Error('Помилка завантаження'));

        
//     }) 

// }

// function delay(ms) {
//     return new Promise((res) => {
//         setTimeout(()=> res(), ms);

//     })
// }

// loadImage('https://wallpaper.forfun.com/fetch/20/2032496c8644d978861803bee90e18c0.jpeg')
//     .then((img) => {return delay(5000).then(() => img)})
//     .then((img) => document.body.append(img))
//     .catch(error => console.error(error));


// Маленький виклик для тебе (Завдання №7):
// Спробуй змінити цей ланцюжок так, щоб спочатку йшла пауза delay(2000),
//  а вже потім починалося завантаження картинки.
// Це покаже тобі, що з промісами ти можеш комбінувати дії як завгодно, як кубики LEGO.

// delay(2000)
//     .then(() => {return loadImage('https://wallpaper.forfun.com/fetch/20/2032496c8644d978861803bee90e18c0.jpeg')})
//     .then((img) => document.body.append(img))
//     .catch(error => console.error(error));


// Завдання №8: «Послідовне фотополювання»
// Тобі потрібно написати ланцюжок, який працює за наступним сценарієм:
// Крок 1: Завантаж першу картинку (використовуй loadImage(url1)).
// Крок 2: Як тільки вона завантажиться, виведи в консоль: "Перша готова, чекаю 3 секунди...".
// Крок 3: Зроби паузу на 3 секунди (використовуй delay(3000)).
// Крок 4: Тільки після паузи почни завантажувати другу картинку (loadImage(url2)).
// Крок 5: Коли завантажиться і друга, виведи в консоль: "Обидві картинки завантажено!" 
// та додай обидві картинки на сторінку (document.body.append).
// 💡 Дві маленькі підказки для успіху:
// Про «пастку» з return: Щоб другий крок чекав на перший, а третій на другий — не забувай писати 
// return перед delay та перед другим loadImage.
// Як не загубити першу картинку: Коли завантажиться друга, тобі захочеться додати в body і першу теж. 
// Ти можеш або створити змінну let img1 зовні ланцюжка, щоб "запам'ятати" її, або спробувати передати
//  її крізь ланцюжок (як ми робили з "сендвічем" return delay(5000).then(() => img)).
// Ось тобі два посилання для роботи:
// URL 1: https://wallpaper.forfun.com/fetch/20/2032496c8644d978861803bee90e18c0.jpeg
// URL 2: https://images.pexels.com/photos/45201/kitty-cat-baby-status-45201.jpeg

// const url1 = 'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTBRXPiXLor0uK712a-VpFyvNI4ppqDP3r5ew&s';
// const url2 = 'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQhb2CFhB8FIMxvTkVr25gGh1McXEMhBkpgoA&s';

// function loadImage(url) {
//     return new Promise((res, rej) => {
//         const newImg = document.createElement('img');
//         newImg.src = url;

//         newImg.onload = () => {res(newImg)};
//         newImg.onerror = () => {rej(new Error('Something went wrong'))};
//     })
// }

// function delay(ms) {
//     return new Promise((res)=> {
//         setTimeout(()=>{res()}, ms);
//     })
// }

// loadImage(url1)
//     .then((img1) => {
//         console.log('Перша готова, чекаю 3 секунди...'); 
//         return delay(3000).then(()=> img1);
//     })
//     .then((img1) => {
//         return loadImage(url2).then((img2)=>[img1, img2]);
//     })
//     .then((imgs) => {
//         console.log('Обидві картинки завантажено!'); 
//         document.body.append(...imgs);
//     })
//     .catch((err)=> console.error(err));



// 🏆 Завдання №9: "Поступовий вхід"
// Уяви, що ти робиш систему авторизації для сайту.
// Тобі потрібно імітувати процес перевірки користувача. У тебе є три етапи, які мають відбутися суворо
//  один за одним:
// Крок 1: Перевірка логіна. Виклич delay(1500). Після паузи виведи в консоль: "Логін перевірено".
// Крок 2: Отримання профілю. Завантаж картинку-аватар (використовуй loadImage(url1)).
// Крок 3: Завантаження налаштувань. Виклич delay(2000). Після паузи виведи в консоль: 
// "Налаштування завантажено".
// Фінал: Тільки коли все пройшло успішно, додай картинку-аватар на сторінку і напиши в консоль: 
// "Ласкаво просимо, користувач!".
// ⚠️ Умови "із зірочкою":
// Використовуй ланцюжок .then().
// Не забувай про return, щоб кожен наступний крок чекав на попередній.
// Передай об'єкт картинки (img) крізь увесь ланцюжок до самого кінця, щоб додати її в body лише 
// на фінальному етапі. (Пам'ятаєш наш "сендвіч" return delay(...).then(() => img)? 
// Він тут дуже знадобиться!).
// Посилання на аватар:
// https://images.pexels.com/photos/614810/pexels-photo-614810.jpeg

// const url1 = 'https://images.pexels.com/photos/614810/pexels-photo-614810.jpeg';

// function loadImg(url) {
//     return new Promise((res, rej) => {
//         const img = new Image();
//         img.src = url;

//         img.onload = ()=>{res(img)};
//         img.onerror = ()=>{new Error('Something went wrong')};
//     })
// }

// function delay(ms) {
//     return new Promise((res) => {
//         setTimeout(()=> res(), ms);
//     })
// }

// delay(1500)
//     .then(()=> {
//         console.log('Логін перевірено'); 
//         return loadImg(url1);
//     })
//     .then((img) => {
//         return delay(2000).then(() => img);
//     })
//     .then((img) => {
//         console.log('Налаштування завантажено');
//         document.body.append(img);
//         console.log('Ласкаво просимо, користувач!');
//     })
//     .catch((err) => console.error(err));


// Завдання №10: "Запуск Гри"
// Тобі потрібно реалізувати ланцюжок, де кожен крок залежить від попереднього:
// Крок 1: Підключення до сервера. Виклич delay(1000). Після цього виведи: "Підключено до сервера".
// Крок 2: Завантаження карти. Завантаж першу картинку (це буде наша локація).
// Крок 3: Рендеринг оточення. Коли карта завантажиться, почекай ще delay(2000). 
// Після паузи виведи: "Оточення готове".
// Крок 4: Завантаження персонажа. Тільки після того, як оточення готове, завантаж другу картинку (персонажа).
// Фінал: Коли персонаж теж завантажиться, виведи: "Гру розпочато!" і додай обидві картинки на сторінку.
// ⚠️ Твої інструменти:
// Два URL (карта та персонаж).
// Сендвічі (щоб не загубити першу картинку, поки вантажиться друга).
// Масив (щоб передати обидві картинки у фінальний .then).
// Посилання для гри:
// Карта (url1): https://images.pexels.com/photos/235621/pexels-photo-235621.jpeg
// Персонаж (url2): https://images.pexels.com/photos/45201/kitty-cat-baby-status-45201.jpeg
// Підказка: У другому .then, де ти вантажиш персонажа, тобі треба буде зробити щось на кшталт:
// return loadImg(url2).then(charImg => [mapImg, charImg]).

// const url1 = 'https://images.pexels.com/photos/235621/pexels-photo-235621.jpeg';
// const url2 = 'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQdybqOjbYxICNg56VFFkI7VX7Wesb5RY6YZQ&s';

// function loadImg(url) {
//     return new Promise((res, rej) => {
//         const img = new Image();
//         img.src = url;

//         img.onload = () => res(img);
//         img.onerror = () => rej(new Error('Failed to load image'));
//     })
// }

// function delay(ms) {
//     return new Promise(res => {
//         setTimeout(()=>{res()}, ms);
//     })
// }

// delay(1000)
//     .then(() => {
//         console.log('пІдключено до сервера');
//         return loadImg(url1);
//     })
//     .then((mapImg) => {
//         return delay(2000).then(()=> mapImg);
//     })
//     .then((mapImg) => {
//         console.log('Оточення готове');
//         return loadImg(url2).then((charImg) => [mapImg, charImg]);
//     })
//     .then((imgs) => {
//         console.log('Гру розпочато!');
//         document.body.append(...imgs);
//     })
//     .catch(err => console.error(err));




// 🏆 Завдання №11: "Публікація поста"
// Сценарій "Завантаження медіа-поста" (як у Instagram чи Facebook). Тобі треба завантажити картинку, 
// отримати текст опису та дочекатися "лайків".
// Тобі потрібно реалізувати ланцюжок, використовуючи зовнішні змінні, щоб код був максимально 
// лінійним (без вкладених .then).
// Твій план дій:
// Створи дві змінні зверху (наприклад, let postImage та let postText).
// Крок 1: Завантаж картинку (loadImg(url1)). Збережи результат у postImage і поверни delay(1000).
// Крок 2: Після паузи виведи в консоль: "Картинку завантажено". Поверни delay(2000).
// Крок 3: Після цієї паузи імітуй отримання тексту: запиши в змінну postText = 
// "Це мій найкращий день у горах!" і поверни ще один delay(1000).
// Фінал: Виведи в консоль: "Пост готовий до публікації!". Додай картинку postImage на 
// сторінку, а під нею (або в консоль) виведи текст postText.
// ⚠️ Правила гри:
// Жодних .then(...) всередині інших .then(...).
// Тільки один довгий ланцюжок, де кожен крок іде суворо під попереднім.
// Використовуй return тільки для пауз (delay).
// Посилання для поста:
// https://images.pexels.com/photos/417074/pexels-photo-417074.jpeg


// let postImg;
// let postText;

// function loadImg(url) {
//     return new Promise((res, rej) => {
//         const img = new Image();
//         img.src = url;

//         img.onload = () => res(img);
//         img.onerror = () => rej(new Error('Failed to load image'))
//     })
// }

// function delay(ms) {
//     return new Promise(res => {
//         setTimeout(() => res(), ms)
//     })
// }

// loadImg('https://images.pexels.com/photos/417074/pexels-photo-417074.jpeg')
//     .then((img) => {
//         postImg = img;
//         return delay(1000);
//     })
//     .then(() => {
//         console.log('Картинку завантажено');
//         return delay(2000);
//     })
//     .then(() => {
//         postText = 'Це мій найкращий день у горах!';
//         return delay(1000)
//     })
//     .then(() => {
//         console.log('Пост готовий до публікації!');
        
//         const text = document.createElement('div');
//         text.textContent = postText;
//         document.body.append(postImg, text);
//     })
//     .catch(err => console.error(err));




                    // Async / await
                    
// Мікро-таск
// Спробуй тепер переписати ту функцію wakeUp, але обов'язково додавши async там, де він має бути.

// function delay(ms) {
//     return new Promise((res => {
//         setTimeout(()=> res(), ms);
//     }))
// }

// // версія Проміс + then:
// // function wakeUp() {
// //     return delay(1000).then(() => {
// //         console.log("Прокинулась!");
// //         return delay(500).then(() => {
// //             console.log("П'ю каву");
// //         });
// //     });
// // }

// // версія async/await
// async function wakeUp() {
//     await delay(3000);
//     console.log('Прокинулась');
//     await delay(2000);
//     console.log("П'ю каву");

// }

// wakeUp();


// Завдання №12: "Реінкарнація Гір" 🏔️📸
// Перепиши свій код публікації поста (Завдання №11), використовуючи async/await.
// Твій алгоритм всередині async function publishPost():
// Створи змінну const img = await loadImg(url1);
// Почекай await delay(1000); і виведи "Картинку завантажено".
// Почекай await delay(2000);.
// Створи змінну const text = "Це мій найкращий день у горах!";.
// Почекай await delay(1000);.
// Виведи фінальний результат на сторінку.
// Використай try...catch щоб перехопити випадок rejected

// const url1 = 'https://images.pexels.com/photos/417074/pexels-photo-4170741.jpeg';

// function loadImg(url) {
//     return new Promise ((res, rej) => {
//         const img = new Image();
//         img.src = url;

//         img.onload = () => res(img);
//         img.onerror = () => rej(new Error("Failed to load image"));
//     })
// }

// function delay(ms) {
//     return new Promise(res => {
//         setTimeout(()=> res(), ms);
//     })
// }

// async function publishPost() {
//     try {
//          // тут loadImg повертає нам Проміс в Result якого лежить картинка, 
//          // а await дістає цю картинку з PromiseResult і віддає її в змінну img
//         const img = await loadImg(url1);
//         await delay(1000);
//         console.log('Картинку завантажено');
//         await delay(2000);
//         const text = 'Це мій найкращий день в горах!';
//         console.log('Текст завантажено');
//         await delay(1000);

//         document.body.append(img, text);
//         await delay(1000);
//         console.log('Пост опубліковано!');
//     } catch(err) {
//         console.error("Ой! Щось пішло не так під час публікації:", err);
//     }
// }

// publishPost();


// Твоє фінальне випробування (якщо не боїшся):
// Уяви, що ти пишеш код для іспанського готелю (пам'ятаєш "mañana"?).
// Тобі треба:
// Завантажити список вільних номерів (await).
// Завантажити ціни (await).
// АЛЕ! Якщо ціни не завантажились, ти не повинна зупиняти все. Ти маєш просто вивести: 
// "Ціни уточнюйте у адміністратора".
// Чи зможеш ти придумати, як оформити try...catch так, щоб помилка в цінах не "вбила" 
// показ списку номерів?


// async function getRooms() {
//     const rooms = ['Standard', 'Luxury', 'Penthouse'];
//     return rooms;
// }

// async function getPrices() {
//     const prices = {
//         standart: 2500, 
//         Luxury: 3000,
//         Penthouse: 5000
//     }
//     return price;  // навмисно повертаю неіснуючу змінну для імітації помилки завантаження щоб спрацював catch
// }

// async function showHotelInfo() {
//     // await кидатиме зауваження await' has no effect on the type of this expression якщо функція,
//     //  яку він викликає не повертає Проміс (або через async або через new Promise)
//     const rooms = await getRooms();
//     const prices = await getPrices().catch(e => {
//         console.error('Помилка завантаження цін');
//         return 'Ціну уточняйте в адміністратора!'
//     })
//     console.log(rooms, prices);
// }

// showHotelInfo();


// Завдання: "Інформаційне табло аеропорту"
// Уяви, що ти розробляєш систему для табло в аеропорту "Бориспіль". 
// Тобі потрібно вивести інформацію про рейс.
// Твої умови:
// Запит №1: Отримати дані про рейс (номер рейсу, місто призначення).
// Запит №2: Отримати статус погоди в місті призначення (щоб пасажири знали, чи брати парасольку).
// Твоя логіка в коді:
// Обидва запити мають запускатися одночасно (паралельно), щоб не змушувати пасажирів чекати.
// Використовуй try...catch для всього процесу.
// АЛЕ: Якщо погода не завантажилася (помилка), табло має все одно показати рейс, 
// а замість погоди написати: "Прогноз тимчасово недоступний".
// Якщо ж не завантажився сам рейс — тоді виводь загальну помилку: "Вибачте, розклад рейсів недоступний".
// 💡 Підказка:
// Тобі знадобиться Promise.all, але щоб один проміс не "вбив" інший, згадай, як ми додавали .catch() 
// прямо до виклику функції в минулому завданні.
// 🛠 Твій "конструктор" :

// // Імітація запиту до API рейсів
// async function fetchFlight() {
//     // Тут може бути помилка, якщо сервер "впав"
//     return { id: 'PS-777', destination: 'Madrid' };
// }

// // Імітація запиту до API погоди
// async function fetchWeather(city) {
//     // Спробуй навмисно викликати тут помилку для перевірки!
//     return "☀️ +25°C";

//     // Щоб catch побачив помилку, нам треба або зробити throw, або (якщо ми в new Promise) зробити rej().
//     // throw new Error("Сервер погоди спить...");

// }

// async function showBoard() {
//     try {
//         const flightInfo = fetchFlight();
//         // Щоб catch побачив помилку, нам треба або зробити throw, або (якщо ми в new Promise) зробити rej().
//         const weatherInfo = fetchWeather('Madrid').catch(e => {
//             console.error('Помилка завантаження погоди:', e.message);
//             return 'Прогноз тимчасово недоступний';
//         });

//         const [flight, weather] = await Promise.all([flightInfo, weatherInfo]);
//         document.body.append(`${flight.id} ${flight.destination} `, weather);

//     } catch (e) {
//         document.body.innerText = 'Вибачте, розклад рейсів недоступний';
//         console.error(e.message);
//     }
// }

// showBoard();


// Реальне завдання: "Стрічка новин"
// Твоя мета — створити функцію, яка завантажує дані про користувача та його пости, і виводить їх на сторінку.
// Умови:
// URL для користувача: https://jsonplaceholder.typicode.com/users/1
// URL для постів: https://jsonplaceholder.typicode.com/posts?userId=1
// Логіка:
// Спочатку завантаж дані про користувача.
// Потім, використовуючи його ім'я, завантаж його пости.
// Виведи на сторінку заголовок: "Пости користувача [Ім'я]".
// Нижче виведи список заголовків його постів (перші 3-5 штук).
// Обробка помилок:
// Використовуй try...catch.
// Обов'язково перевіряй res.ok після кожного fetch. Якщо сервер повернув помилку (наприклад, 404), 
// викидай new Error().
// 💡 Підказка:
// Зверни увагу, що userRes.json() повертає об'єкт, у якого є поле id та name. Тобі знадобиться user.id, 
// щоб сформувати правильний запит до постів.
// Це завдання максимально наближене до того, що роблять Front-end розробники щодня: отримують дані, 
// перевіряють їх і "малюють" на екрані.
// 🛠 Твій стартовий шаблон:

// async function renderNewsFeed() {
//     try {
//         // 1. Завантажуємо користувача
//         const userRes = await fetch('https://jsonplaceholder.typicode.com/users/1');
//         if (!userRes.ok) throw new Error('Користувача не знайдено');
//         const user = await userRes.json();

//         // 2. Тепер завантажуємо його пости (використовуй userId з об'єкта user)
//         const userResPosts = await fetch(`https://jsonplaceholder.typicode.com/posts?userId=${user.id}`);
//         if (!userResPosts.ok) throw new Error('Пости користувача не знайдено')
//         const userPosts = await userResPosts.json();
        
//         // 3. Виведи результат у DOM (наприклад, через innerHTML або append)
//         const title = document.createElement('h2');
//         title.textContent = `Пости користувача ${user.name}`;
//         document.body.append(title);

//         // варіант 1
//         // userPosts.slice(0, 5).forEach(post => {
//         //     let item = document.createElement('div');
//         //     item.textContent = post.title;
//         //     document.body.append(item);
//         // });

//         // варіант 2
//         userPosts.forEach(post => { if (userPosts.indexOf(post) < 5) {

//             let item = document.createElement('div');
//             item.textContent = post.title;
//             document.body.append(item);

//         }})


//     } catch (error) {
//         console.error("Сталася помилка:", error.message);
//         document.body.innerText = "Упс! Не вдалося завантажити стрічку новин.";
//     }
// }

// renderNewsFeed();



// Завдання: "Валютний інформатор НБУ"
// Національний банк України (НБУ) має чудове відкрите API, яке не потребує реєстрації чи ключів. 
// Твоя мета:
// Створити функцію, яка завантажує актуальний курс Долара (USD) та Євро (EUR) стосовно гривні та 
// виводить їх на екран.
// Твої інструменти (API НБУ):
// URL для всіх валют: https://bank.gov.ua/NBUStatService/v1/statdirectory/exchange?json
// Цей запит повертає масив об'єктів. Кожен об'єкт виглядає приблизно так:
// { "txt": "Долар США", "rate": 39.5, "cc": "USD" }
// Що треба зробити:
// Зробити fetch за вказаним посиланням.
// Перевірити res.ok.
// Отримати масив даних через .json().
// Відфільтрувати цей масив (метод .filter() або .find()), 
// щоб знайти об'єкти, де cc === 'USD' та cc === 'EUR'.
// Вивести їх на сторінку в такому вигляді:
// Курс валют на сьогодні:
// 🇺🇸 USD: [значення rate] грн
// 🇪🇺 EUR: [значення rate] грн
// Додати try...catch для обробки помилок (наприклад, якщо пропав інтернет).
// 🛠 Маленька підказка щодо фільтрації:
// Оскільки даних прийде багато (всі валюти світу), тобі треба буде знайти потрібні. Наприклад:
// const usd = data.find(item => item.cc === 'USD');
// console.log(usd.rate);

// async function getNbuUsdEurExchangeRates() {
//     try {
//         const result = await fetch('https://bank.gov.ua/NBUStatService/v1/statdirectory/exchange?json');
//         if (!result.ok) throw new Error('Проблема завантажеення курсів валют');
//         const exchangeRatesAll = await result.json();
        

//         const specificExchangeRates = exchangeRatesAll.filter(specificRate => {
//             specificRate.cc === 'USD' || specificRate.cc === 'EUR';
//         })

//         return specificExchangeRates;
//     } catch (e) {
//         console.error('Щось пішло не так: ', e.message);
//     }
// }


// async function appendNbuUsdEurExchangeRates() {
//     // викор await тому що getNbuUsdEurExchangeRates поверне Проміс, а await дістане з нього результат
//     // також для того щоб дочекатись поки getNbuUsdEurExchangeRates поверне результат і не виконувати без
//     // нього код нижче
//     const exchangeRates = await getNbuUsdEurExchangeRates();

//     const title = document.createElement('h3');
//     title.textContent = 'Курс валют на сьогодні :';
//     document.body.append(title);

//     const list = document.createElement('ul');
//     document.body.append(list);

//     if (!exchangeRates) {
//         console.error('Проблема відображення курсів валют');
//         const listItem = document.createElement('li');
//         listItem.textContent = `Curr: 00.00 грн.`;
//         list.append(listItem);
//         return;
        
//     }

//     exchangeRates.forEach(rate => {
//         const listItem = document.createElement('li');
//         listItem.textContent = `${rate.cc}: ${rate.rate} грн.`;
//         list.append(listItem)
//     })
// }

// appendNbuUsdEurExchangeRates();




// Нове завдання: "Конвертер для поїздок"
// Уяви, що ти збираєшся в подорож.
// Завантаж курс EUR з того ж API НБУ.
// Уяви, що в тебе в кишені є 1000 грн.
// Обчисли, скільки євро ти зможеш купити (1000 / курс).
// Виведи на сторінку: "За 1000 грн ти купиш [результат] EUR".
// Округли результат до 2 знаків після коми (використовуй .toFixed(2)).
// Це навчить тебе не просто показувати цифри, а використовувати їх у логіці! Готова до математики? 😉💸🇪🇺

// async function getExchangeRate(rate) {
//     try {
//         const exchangeRatesResult = await fetch('https://bank.gov.ua/NBUStatService/v1/statdirectory/exchange?json');
//         if (!exchangeRatesResult.ok) throw new Error('Failed to load exchange rates');
//         const exchangeRates = await exchangeRatesResult.json();

//          return exchangeRates.find(item => item.cc === rate);
//     } catch (err) {
//         console.error('Щось пішло не так: ', err.message);
//     }

// }

// async function createExchangeOperation(sum, rate) {
//     const exchangeRate = await getExchangeRate(rate);

//     const paragraph = document.createElement('p');
//     if (!exchangeRate) {
//         paragraph.textContent = `За 0 грн ти купиш 0 ${rate}`;
//         document.body.append(paragraph);
//         return;
//     }

//     const exchangeResult = (sum / exchangeRate.rate).toFixed(2);

//     paragraph.textContent = `За ${sum} грн ти купиш ${exchangeResult} ${rate}`;
//     document.body.append(paragraph);

// }

// createExchangeOperation(1000, 'HUF');


// Твоє нове випробування (Challenge):
// Спробуй переписати свій код так, щоб:
// Була функція fetchAllData(), яка тільки робить fetch і повертає масив.
// Була функція showAllCalculations(), яка один раз викликає fetchAllData, отримує масив, і 
// за допомогою цього масиву виводить на екран розрахунки для USD, EUR та PLN (злоті).
// Я хочу, щоб ти змінила логіку так, щоб мережевий запит став "одноразовим актом", 
// а розрахунки — "масовим виробництвом".
// Ось покроковий план, що має відбуватися всередині showAllCalculations():
// Один єдиний await: Ти викликаєш fetchAllData() і отримуєш один великий масив усіх валют. 
// Все, інтернет нам більше не потрібен.
// Локальна робота: Далі, маючи цей масив "на руках", ти використовуєш його тричі (або десять разів), 
// щоб знайти курс долара, євро та злотого.
// Миттєвий результат: Ти виводиш на екран відразу всі три результати (наприклад, три параграфи або 
//     один список).
// 🏗 Структура, яку я від тебе чекаю (схематично):
// Функція А (fetchAllData):
// Просто йде в НБУ.
// Повертає масив.
// Більше нічого не знає і нікуди нічого не "апендить".
// Функція Б (наприклад, calculateOne):
// Не асинхронна!
// Приймає суму, код валюти та той самий масив, який ми завантажили раніше.
// Просто повертає цифру (результат).
// Функція В (showAllCalculations):
// Це "диригент".
// Вона викликає Функцію А (чекає результат).
// Потім вона тричі викликає Функцію Б, передаючи їй завантажений масив.
// В кінці вона виводить все це "добро" в DOM.
// Суть завдання: Зробити так, щоб програма "сходила в магазин" (в інтернет) один раз, купила велику 
// пачку продуктів (масив валют) і вже вдома приготувала з них три різні страви (курси USD, EUR, PLN),
//  не вибігаючи за кожним інгредієнтом окремо.

// функція для завантаження та розпаковування даних з сервера, повертає отримані дані (arr, obj)
// викличеться один раз з головної функції щоб при кожній операції не звертатись до сервера
// async function fetchAllData() {
//     const result = await fetch('https://bank.gov.ua/NBUStatService/v1/statdirectory/exchange?json');

//     // Примусово роблю помилку 404 щоб перевірити чи відпрцює мій throw
//     // const result = {ok: false, status: 404};
//     // console.log("Статус відповіді:", result.status, "Чи все ОК:", result.ok);

//     if (!result.ok) throw new Error('Помилка завантаження даних');
//     const allData = await result.json();

//     return allData;
// }

// ця функція використовувалась би клієнтом який тицяє валюти на екрані
// // синхронна функція яка порсто робить розрахунок з тих параметрів які отримала і повертає число
// function calculateOne(sum, curr, data) {
//     const rate = data.find(item => item.cc === curr);

//     return rate ? (sum / rate.rate).toFixed(2) : 'N/A';
// }

// ця функція запуститься один раз при завантаженні сторінки, клієнт її не тицяє
// // головна функція - вона один раз завантажує дані з сервера а потім отриманий масив передає в функцію
// // калькулятор і рендерить отримані розрахунки
// async function showAllCalculations() {
//     try {
//         const dataArr = await fetchAllData();

//         const usd = calculateOne(1000, 'USD', dataArr);
//         const eur = calculateOne(1000, 'EUR', dataArr);
//         const pln = calculateOne(1000, 'PLN', dataArr);
//         const mars = calculateOne(1000, 'MARS', dataArr);  // неіснуюча валюта  має вивести N/A а не зламати код

//         document.body.append(usd, eur, pln, mars);
//     } catch (e) {
//         console.error('Щось пішло не так: ', e.message);
//         document.body.innerText = "Вибачте, сервіс тимчасово недоступний (помилка мережі).";
//     }

// }

// showAllCalculations();


// Завдання: "Розумний фільтр бібліотеки"
// Уяви, що ми отримуємо з сервера список книг. Але нам не потрібні всі — ми хочемо знайти тільки 
// "перлини" для твоєї паперової полиці.
// Твоя мета:
// Написати async функцію fetchBooks(), яка імітує запит до сервера (ми використаємо фейковий масив).
// Написати функцію filterBestBooks(books), яка знайде книги за двома критеріями:
// Жанр — "Romance".
// Рейтинг — більше 4.5.
// Вивести результат на сторінку у вигляді гарного списку.
// Ось твій "фейковий" сервер (скопіюй це в fetchBooks):
// const mockBooks = [
//     { title: "Beach Read", author: "Emily Henry", genre: "Romance", rating: 4.8 },
//     { title: "The Love Hypothesis", author: "Ali Hazelwood", genre: "Romance", rating: 4.7 },
//     { title: "Project Hail Mary", author: "Andy Weir", genre: "Sci-Fi", rating: 4.9 },
//     { title: "The Spanish Love Deception", author: "Elena Armas", genre: "Romance", rating: 4.2 },
//     { title: "Normal People", author: "Sally Rooney", genre: "Drama", rating: 4.0 }
// ];
// 🏗 Структура коду (Challenge):
// fetchBooks(): Має повертати Promise з масивом mockBooks (можеш використати return mockBooks; 
//     всередині async функції).
// renderBooks(): "Диригент". Викликає завантаження, потім фільтрує масив (використовуй .filter()) 
// і малює в DOM тільки ті книги, що підійшли.
// Чому це важливо?
// Це навчить тебе не просто шукати один елемент (як ми робили з find для валют), а вибирати групу 
// елементів за складними умовами.


// async function fetchBooks() {

    // const mockBooks = [
    //     { title: "Beach Read", author: "Emily Henry", genre: "Romance", rating: 4.8 },
    //     { title: "The Love Hypothesis", author: "Ali Hazelwood", genre: "Romance", rating: 4.7 },
    //     { title: "Project Hail Mary", author: "Andy Weir", genre: "Sci-Fi", rating: 4.9 },
    //     { title: "The Spanish Love Deception", author: "Elena Armas", genre: "Romance", rating: 4.2 },
    //     { title: "Normal People", author: "Sally Rooney", genre: "Drama", rating: 4.0 }
    // ];
//     if (!mockBooks) throw new Error('Помилка завантаження книжок');
//     return mockBooks;
// }

// function filterBestBooks(books) {
//     const bestBooks = books.filter(book => book.genre === 'Romance' && book.rating > 4.5);
//     if (!bestBooks || bestBooks.length === 0) throw new Error('Не знайдено відповідної книжки');
//     return bestBooks;

// }


// async function renderBooks() {
//     try {
//         const books = await fetchBooks();
//         const filteredBooks = filterBestBooks(books);

//         const title = document.createElement('h3');
//         title.textContent = `Книги жанру ${filteredBooks[0].genre} з найвищим рейтингом:`;

//         const list = document.createElement('ol');

//         document.body.append(title, list);

//         filteredBooks.forEach(({title, author, rating}) => {
//             const item = document.createElement('li');
//             item.textContent = `Книга ${title} автора ${author} (рейтинг: ${rating})`;
//             list.append(item);

//         })
        

//     } catch (e) {
//         console.log('Щось пішло не так:', e.message);
//         document.body.innerText = 'Запитуваних книжок не знайдено !'
//     }

// }

// renderBooks();


// Кольорове завдання: "Розумна бібліотека"
// Твоя мета — зробити так, щоб список книг сам "підказував" користувачу, що варто читати,
//  а що ні, за допомогою кольорів та тексту.
// 1. Підготовка даних (fetchBooks):
// Використовуй той самий масив mockBooks, що й минулого разу (Emily Henry, Ali Hazelwood тощо).
// 2. Логіка відображення (renderBooks):
// Замість того, щоб видаляти "погані" книги через filter, виведи всі книги з масиву, 
// але застосуй до них правила:
// Якщо рейтинг < 4.5:
// Колір тексту має стати сірим (item.style.color = 'gray').
// В кінці назви додай текст: (Мало зірок ⭐️).
// Якщо рейтинг >= 4.5:
// Зроби текст жирним (item.style.fontWeight = 'bold').
// (Опціонально) Можеш додати колір, наприклад, темно-зелений.
// 3. Обробка "Порожньої полиці":
// Перед тим як малювати список, перевір: чи не порожній масив?
// Якщо в масиві 0 елементів — не викидай помилку через throw, а просто виведи на екран напис: 
// "На жаль, бібліотека порожня ☕".


// async function fetchBooks() {
//     const mockBooks = [
//         { title: "Beach Read", author: "Emily Henry", genre: "Romance", rating: 4.8 },
//         { title: "The Love Hypothesis", author: "Ali Hazelwood", genre: "Romance", rating: 4.7 },
//         { title: "Project Hail Mary", author: "Andy Weir", genre: "Sci-Fi", rating: 4.9 },
//         { title: "The Spanish Love Deception", author: "Elena Armas", genre: "Romance", rating: 4.2 },
//         { title: "Normal People", author: "Sally Rooney", genre: "Drama", rating: 4.0 }
//     ];

//     if (!mockBooks) throw new Error('Не вдалось завантажити книги');
//     return mockBooks;
// }


// async function renderBooks() {
//     try {
//         const books = await fetchBooks();

//         if (!books || books.length === 0) {
//             const emptyMessage = document.createElement('div');
//             emptyMessage.textContent = 'На жаль, бібліотека порожня ☕';
//             document.body.append(emptyMessage);
//             return;
//         }

//         const booksList = document.createElement('ol');
//         document.body.append(booksList);

//         books.forEach(({title, author, rating}) => {
//             const listItem = document.createElement('li');
//             listItem.textContent = `Книга ${title} автора ${author} з рейтингом: ${rating.toFixed(1)}`;
//             if (rating >= 4.5) {
//                 listItem.style.fontWeight = 'bold';
//                 listItem.style.color = 'darkGreen';
//             } else {
//                 listItem.style.color = 'gray';
//                 listItem.textContent += ' (Мало зірок ⭐️)';
//             }
//             booksList.append(listItem);

//         })
//     } catch (e) {
//         console.error('Щось пішло не так: ', e.message);
//         document.body.innerText = 'Щось пішло не так! Спробуйте ще раз.'
//     }

// }

// renderBooks();